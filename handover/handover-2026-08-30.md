# Handover — 2026-08-30 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última
atualização: 20h50

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` →
> `handover/handover-2026-08-12.md` (contexto do ticket 027, ainda pendente) → este documento
> → `wayfinder/README.md`.
>
> ✅ **Ticket 003 fechado — exportação de conversas inviável, substituída pela análise do dono
> do projeto via grilling.** Troca deliberada de fidelidade por desbloqueio. Desbloqueou 010,
> 013, 014. Ver `## Resolução` no ticket e a nota no mapa antes de tratar tom ou aprendizado.
>
> 🆕 **Planilha de clientes das consultoras disponível** — o dono do projeto exportou e subiu;
> está em `dados/CARTEIRA+MAILLING.xlsx` (pasta `/dados/` ignorada pelo git, é dado pessoal —
> LGPD). **Ainda não foi inspecionada** — nenhuma sessão abriu o arquivo. O ticket 004 **segue
> aberto**: falta inspecionar essa planilha, o catálogo do Maino e exemplos de planilha de
> arquiteto. **Recomendação para a próxima sessão: começar pelo 004** — ver "Pendências".
>
> ✅ **Ticket 010 fechado — o que é um lead qualificado e onde os dados vão.** Agente não é
> formulário. Campos núcleo (nome, o que procura, para quando, modo) + oportunistas; nada
> obrigatório a ponto de barrar um contato. Arquiteto → escala imediata sem coletar. Cliente
> conhecido → qualificação leve, escala para a dona. **Supabase é a memória interna do agente**
> (todo atendimento, inclusive os perdidos), as consultoras não mexem nele; o chamado vira um
> **relance** na fila do 012, nunca a conversa (essa fica no WhatsApp). `Atendimento
> qualificado` entrou no `CONTEXT.md`. Ver `## Resolução` no ticket.
>
> ⚠️ **PR da branch `wayfinder/atendimento-hoje` continua sem abrir** — `gh` ainda não
> instalado nesta máquina.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch
`wayfinder/atendimento-hoje`, PR ainda não aberto, `main` atrasada (todo o wayfinder acumula
nesta branch). Local == `origin/wayfinder/atendimento-hoje` em `b621e9f` — tudo empurrado,
nada pendente. Três commits nesta sessão: `e682ebc` (fecha 003 + reivindica 010), `4cd201e`
(resolve 010), `b621e9f` (`.gitignore` contra planilhas). Único untracked: `.claude/` (config
local, não é do projeto). `dados/CARTEIRA+MAILLING.xlsx` fora do git por design.

## Pendências / próximos passos

1. **COMEÇAR PELO TICKET 004.** A planilha de clientes agora existe em
   `dados/CARTEIRA+MAILLING.xlsx`. Inspecionar: colunas de cada aba, quão preenchidas, se o
   formato é igual entre consultoras, quantos clientes. **Registrar só o padrão na resolução —
   nunca os valores (LGPD).** Conferir se as colunas batem com os campos que o 010 definiu (a
   resolução do 010 assumiu isso sem ver o cabeçalho).
   - **Decidir com o dono do projeto: dividir o 004.** Um ticket "planilha de clientes" (fecha
     assim que inspecionada) e outro "Maino + planilha de arquiteto" (fica aberto). Isso
     **destrava 030 e 031** sem esperar o Maino — eles só precisam da planilha compartilhada.
   - **030/031 vão precisar de acesso de escrita à planilha viva do Google** (Sheets API /
     Apps Script). O `.xlsx` dá a estrutura, não o acesso — esse acesso é sub-tarefa do
     004-dividido ou do 031.
   - **011 continua bloqueado** mesmo com o 004 dividido — ele precisa do Maino, não da
     planilha de clientes.
2. **Depois do 004: ticket 013** (sinal de sucesso). Grilling, desbloqueado, se apoia na mesma
   análise do dono. A planilha ajuda na atribuição e no sinal "já é cliente". Dependência do
   012: a janela de retomada = limiar de "contato perdido", decidir um número só serve os dois.
3. **Alternativas independentes:** 014 (protótipo de tom) e 015 (rotação de credenciais) — não
   ganharam nem perderam com a planilha.
4. **Round 3 do 010 não reabre o 012:** o conteúdo exato do relance da fila foi decidido na
   resolução do 010; os tickets 030/031 herdam essa spec.
5. **Ticket 027 segue pendente** — bola com o dono do projeto (chip + dispositivos). Ver
   handover de 2026-08-12.
6. **Fronteira agora:** `004` (recomendado), `013`, `014`, `015`, `017`, `020`, `021`, `027`
   (reivindicado). Bloqueados: `011` (004), `018` (017), `019` (pausa), `030`/`031` (004).

## Decisões e notas

- **Ticket 003 — exportação inviável, substituída pela análise do dono do projeto.** O dono
  não conseguiu extrair os `.txt` do WhatsApp Business. Decisão dele: usar a própria análise
  de como as consultoras respondem, alimentada nas sessões de grilling (010, 013) e no
  protótipo (014). O que se perde: transcrição literal — sem corpus real para o 014 comparar
  linha a linha nem material bruto para o laço de aprendizado calibrar depois. O tom do 014
  passa a sair da descrição do dono + amostra da planilha, como ponto de partida, não
  referência definitiva. Se as conversas aparecerem depois, revisar 010/013/014.
- **`.gitignore` endurecido contra planilhas.** A carteira de clientes apareceu untracked na
  raiz e não batia em nenhum padrão. Agora `*.xlsx/*.xls/*.xlsm/*.csv/*.ods` ficam fora do git
  em qualquer caminho, além de `/dados/` já existente. O arquivo foi movido para `dados/`.

---

## Sessões do dia

### 19h40–20h50 — Ticket 003 fechado, ticket 010 resolvido por grilling

- Fronteira revisada com o dono do projeto. Duas informações novas: (a) acesso à planilha de
  clientes liberado; (b) exportação de conversas não conseguida, mas o dono analisou como as
  consultoras respondem e se propôs a descrever via grilling.
- Decisão do dono: **reformular e fechar o 003** aceitando a análise como substituto (troca de
  fidelidade por desbloqueio) e **resolver o ticket 010 nesta sessão**.
- Executado: `## Resolução` no ticket 003, `status: closed`; 010 reivindicado; README e map
  atualizados (003 em Fechados; 010/013/014 saem de Bloqueados; nota sobre 004 parcial).
  Commit `e682ebc`.
- **Grilling do 010, 3 rounds.** Fechado com: agente não é formulário (4 regras de condução,
  redação fica para o 014); lista de campos núcleo + oportunistas; nada obrigatório a ponto de
  barrar contato; arquiteto escala na hora sem coletar nada (sem o acréscimo que eu havia
  proposto — o dono cortou); cliente conhecido → qualificação leve + escala para a dona;
  qualificação completa = núcleo suficiente para a consultora assumir sem recomeçar; destino
  dos dados = Supabase interno do agente (todo atendimento, inclusive perdidos) + relance na
  fila do 012 + nunca nas abas das consultoras; a linha da fila não reproduz a conversa (fica
  no WhatsApp), só o relance para priorizar.
- `## Resolução` no ticket 010, `status: closed`; `CONTEXT.md` ganhou `Atendimento qualificado`
  e perdeu "Lead qualificado" de "Ainda sem definição"; `map.md` (Decisions so far + poda da
  névoa "Modelo de dados no Supabase") e `README.md` atualizados. Commit `4cd201e`.
- **Fechamento da sessão** (a pedido do dono, para poder dar `/clear`): `CARTEIRA+MAILLING.xlsx`
  movido para `dados/`; `.gitignore` endurecido contra `*.xlsx/*.xls/*.xlsm/*.csv/*.ods`
  (commit `b621e9f`); tudo empurrado; handover finalizado como ponto de entrada limpo para a
  próxima sessão. **Recomendação registrada: próxima sessão começa pelo ticket 004.**
