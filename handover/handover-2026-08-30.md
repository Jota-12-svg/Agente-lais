# Handover — 2026-08-30 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última
atualização: 21h30

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` →
> `handover/handover-2026-08-12.md` (contexto do ticket 027, ainda pendente) → este documento
> → `wayfinder/README.md`.
>
> ✅ **Ticket 004 fechado e dividido.** A planilha `dados/CARTEIRA+MAILLING.xlsx` foi
> inspecionada (só estrutura, nenhum valor — LGPD). **Descoberta que importa:** o arquivo é,
> na maior parte, um **diretório de arquitetos + mailing + registro de relacionamento**, não
> um CRM de consumidor final. **O "lookup por telefone: já é cliente?" que o ticket 010
> assumiu não se sustenta para consumidor final** — onde há telefone confiável, são
> arquitetos. Catálogo do Mainô e planilhas de arquiteto foram para o **ticket 032** (novo,
> aberto). Ver `## Resolução` no 004.
>
> 🔸 **Confirmado pelo dono do projeto:** o `.xlsx` **é** a planilha viva (cópia para trabalho
> local; o acesso ao Google Sheets das consultoras vem no setup do agente na loja). A loja tem
> 4 consultoras, **só 3 atendem** — as 3 abas estão certas. `CONTEXT.md` corrigido nesta sessão
> (entradas `advisor` e `shared_sheet`).
>
> 🔸 **Ainda em aberto:** a **resolução do ticket 010 precisa de um ajuste** — o lookup "já é
> cliente" por telefone não alcança consumidor final (onde há telefone confiável, são
> arquitetos). O que resta é casar o número contra o diretório de arquitetos como sinal de
> classificação "é arquiteto". É decisão do dono do projeto, fora do escopo do 004.
>
> ⚠️ **PR da branch `wayfinder/atendimento-hoje` continua sem abrir** — `gh` ainda não
> instalado nesta máquina.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch
`wayfinder/atendimento-hoje`, PR ainda não aberto, `main` atrasada (todo o wayfinder acumula
nesta branch). Antes desta sessão: local == `origin` em `b621e9f`. Esta sessão adiciona um
commit fechando o 004 e reorganizando o tracker (nenhum dado de cliente tocado; a planilha
segue fora do git em `dados/`). Único untracked fora isso: `.claude/` (config local).

## Pendências / próximos passos

1. **Fronteira agora:** `013`, `014`, `015`, `017`, `020`, `021`, `027` (reivindicado), e os
   recém-desbloqueados `030`, `031`, mais o novo `032`.
   - **Bloqueados:** `011` (agora por **032**, não mais 004), `018` (017), `019` (em pausa).
2. **030 e 031 saíram de bloqueados** com o fechamento do 004. **Acesso de edição à planilha
   viva do Google vem no setup do agente na loja** (definido pelo dono). O desenho de
   autenticação de escrita (031) e o formato do e-mail (030) podem adiantar agora; o teste de
   ponta a ponta espera o acesso. Cada um ganhou uma seção "Nota — inspeção do ticket 004".
3. **Ticket 032** (catálogo do Mainô + planilhas de arquiteto): o research 007 já confirmou
   que o Mainô tem `GET /produtos`; falta puxar uma amostra real e ver o que vem. **Não há
   credencial do Mainô no `.env`** — 032 vai precisar dela do dono, ou de uma exportação
   manual. As planilhas de arquiteto se sobrepõem à pergunta 32 do ticket 020.
4. **Ajuste na resolução do ticket 010** (decisão do dono): o lookup "já é cliente" por
   telefone não alcança consumidor final. O que o agente faz é casar o número contra o
   **diretório de arquitetos** como sinal de classificação "é arquiteto" (útil para 011/012),
   não como "já é cliente". Pode virar um round rápido de grilling no 010 ou um addendum.
5. **Ticket 013** (sinal de sucesso) ganhou um insumo: a aba `Entregas` da planilha tem
   `Status` (`Vendido`/`Reservado`/`Em produção`) + `Valor` + `# Pedido` — registro de venda
   do lado da loja, possível ponte de atribuição com a nota fiscal do Mainô.
6. **Ticket 027 segue pendente** — bola com o dono do projeto (chip + dispositivos). Ver
   handover de 2026-08-12.

## Decisões e notas

- **Ticket 004 — a planilha não é o que o mapa assumia.** `CARTEIRA+MAILLING.xlsx`: 8 abas.
  `LISTA DE PROFISSIONAIS COMPLETA` (1283 escritórios, contato quase completo, sem dono
  atribuído) + `MAILING` (584, sem telefone) + `GERAL` (999 arquitetos, ~26%) são o grosso.
  As 3 abas de consultora (PAMELLA, GABRIELA, JOSLAINE — **nada para a 4ª pessoa/a dona**) são
  lista de arquiteto + log de brindes/aniversários, esparsas (0–16%) e **com formatos
  diferentes entre si** (JOSLAINE é layout à parte, com histórico de contato em texto livre).
  `VISITAS` e `Entregas` são logs operacionais quase vazios nesta exportação. Detalhe completo
  na `## Resolução` do ticket.
- **Divisão do 004.** Parte (a) planilha de clientes → fechou no 004. Partes (b) catálogo do
  Mainô e (c) planilhas de arquiteto → ticket **032**, aberto. `011` foi repontado de `004`
  para `032` no `blocked-by`.
- **Ticket 003 — exportação inviável, substituída pela análise do dono do projeto.** (Mantido
  da sessão anterior.) Se as conversas aparecerem depois, revisar 010/013/014.
- **`.gitignore` endurecido contra planilhas** (sessão anterior): `*.xlsx/*.xls/*.xlsm/*.csv/*.ods`
  fora do git em qualquer caminho, além de `/dados/`.

---

## Sessões do dia

### 19h40–20h50 — Ticket 003 fechado, ticket 010 resolvido por grilling

- Fronteira revisada com o dono do projeto. Duas informações novas: (a) acesso à planilha de
  clientes liberado; (b) exportação de conversas não conseguida, mas o dono analisou como as
  consultoras respondem e se propôs a descrever via grilling.
- Decisão do dono: **reformular e fechar o 003** aceitando a análise como substituto (troca de
  fidelidade por desbloqueio) e **resolver o ticket 010 nesta sessão**.
- Executado: `## Resolução` no ticket 003, `status: closed`; 010 reivindicado; README e map
  atualizados (003 em Fechados; 010/013/014 saem de Bloqueados; nota sobre 004 parcial).
  Commit `e682ebc`.
- **Grilling do 010, 3 rounds.** Fechado com: agente não é formulário (4 regras de condução,
  redação fica para o 014); lista de campos núcleo + oportunistas; nada obrigatório a ponto de
  barrar contato; arquiteto escala na hora sem coletar nada; cliente conhecido → qualificação
  leve + escala para a dona; destino dos dados = Supabase interno do agente + relance na fila
  do 012 + nunca nas abas das consultoras.
- `## Resolução` no ticket 010, `status: closed`; `CONTEXT.md` ganhou `Atendimento qualificado`;
  `map.md` e `README.md` atualizados. Commit `4cd201e`.
- **Fechamento da sessão:** `CARTEIRA+MAILLING.xlsx` movido para `dados/`; `.gitignore`
  endurecido (commit `b621e9f`); tudo empurrado. Recomendação registrada: próxima sessão
  começa pelo ticket 004.

### 21h00–21h30 — Ticket 004 inspecionado, fechado e dividido

- Fronteira revisada. Dono aprovou a divisão do 004 (planilha de clientes fecha; catálogo +
  planilha de arquiteto viram ticket novo).
- **Inspeção de `dados/CARTEIRA+MAILLING.xlsx`** via `openpyxl`, leitura só de estrutura
  (nomes de aba, cabeçalhos, taxa de preenchimento, tipos) — nenhum valor de cliente
  registrado. Scripts descartáveis no scratchpad.
- Descobertas principais: arquivo é diretório de arquitetos + mailing + log de
  relacionamento, não CRM de consumidor final; formato não uniforme entre consultoras; só 3
  abas de consultora; sem aba isolada de datas importantes; **lookup "já é cliente" por
  telefone do ticket 010 não se sustenta para consumidor final**. Insumo para o 013 na aba
  `Entregas` (Status/Valor/# Pedido).
- Executado:
  - `004` reescrito (escopo reduzido à planilha), `## Resolução` completa, `status: closed`,
    `assignee: Claude`.
  - `032` criado (catálogo do Mainô + planilhas de arquiteto), aberto, `blocked-by: []`.
  - `011` repontado: `blocked-by` de `["009","004"]` para `["009","032"]`.
  - `030` e `031` ganharam seção "Nota — inspeção do ticket 004" (aba de fila não existe;
    e-mails das consultoras não estão na exportação; acesso de edição ainda pendente).
  - `map.md`: linha em "Decisions so far" para o 004; névoa "Fluxo do arquiteto" aponta para
    o 032.
  - `README.md`: 004 para Fechados; 030/031/032 para a Fronteira; 011 bloqueado por 032.
- **Dono confirmou** (segunda rodada da sessão): o `.xlsx` é a planilha viva (acesso ao Google
  Sheets vem no setup); 4 consultoras, só 3 atendem → 3 abas está certo.
  - `CONTEXT.md` corrigido: entradas `advisor` (3 das 4 atendem) e `shared_sheet` (descrição
    real da planilha + cópia local + acesso no setup).
  - `004`: seção "Confirmado com o dono" adicionada; pendências reduzidas a duas (acesso de
    edição no setup; ajuste no 010).
  - `map.md` e handover atualizados. Continua em aberto: o ajuste na resolução do **010**
    (lookup "já é cliente" não alcança consumidor final).
