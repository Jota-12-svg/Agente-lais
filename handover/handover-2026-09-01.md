# Handover — 2026-09-01 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última
atualização: (sessão em curso)

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` →
> `handover/handover-2026-08-12.md` (contexto do ticket 027, ainda pendente) →
> `handover/handover-2026-08-30.md` → este documento → `wayfinder/README.md`.
>
> ✅ **Addendum do ticket 010 fechado por grilling.** A ponta solta que o 004 deixou (o lookup
> por telefone não alcança consumidor final) foi resolvida. Três decisões: (1) o lookup vira
> **match best-effort contra o diretório de arquitetos** — sinal de classificação, não "já é
> cliente"; (2) **"cliente conhecido → escala para a dona" cai para consumidor final** — fase 1
> trata todo consumidor final como contato novo (rodízio), reconhecer cliente pré-agente é da
> consultora; (3) **a pergunta do modo passa a ser deliberada** — turno 1 sempre, as duas
> categorias (casa própria / arquiteto) nomeadas explicitamente, redação exata no 014. Ticket
> 010 continua `closed`; addendum documenta a revisão. Ver `## Addendum` no 010.
>
> ⏭️ **Próximo recomendado: ticket 017** (decidir provedor de LLM + habilitar billing). A
> decisão técnica já está no 008 (Gemini API paga da Google); falta o dono bater o martelo e
> habilitar o billing. Desbloqueia o 018.
>
> ⚠️ **PR da branch `wayfinder/atendimento-hoje` continua sem abrir** — `gh` ainda não
> instalado nesta máquina. O wayfinder inteiro acumula nesta branch.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch
`wayfinder/atendimento-hoje`, PR ainda não aberto, `main` atrasada. Antes desta sessão: local
== `origin` em `8deb76b`. Esta sessão adiciona um commit com o addendum do 010 + ajustes no
`CONTEXT.md` e no `map.md`. Nenhum dado de cliente tocado. Untracked: `.claude/` (config
local, não versionar).

## Pendências / próximos passos

1. **Fronteira agora:** `013`, `014`, `015`, `017`, `020`, `021`, `027` (reivindicado),
   `030`, `031`, `032`.
   - **Bloqueados:** `011` (por 032), `018` (por 017), `019` (em pausa).
2. **017 é o próximo recomendado** — decisão pronta no 008, falta martelo do dono + billing.
3. **Vários tickets dependem do dono:** 015 (rotação de credenciais), 020 (perguntas às
   consultoras), 021 (Instagram), 027 (chip + dispositivos), 032 (credencial do Mainô),
   030/031 (acesso de edição ao Google Sheets — vem no setup na loja).
4. **Redação da pergunta do modo** entra no escopo do **014**: turno 1 sempre, as duas
   categorias nomeadas ("é para a sua casa ou você é arquiteto/designer montando um projeto
   para um cliente?"), pulada só quando o cliente já se classificou sozinho.

## Decisões e notas

- **Addendum do 010 (2026-09-01)** — detalhe completo na seção `## Addendum` do ticket. Em
  resumo:
  - **Lookup por telefone** → só sobrevive como match contra o **diretório de arquitetos**
    (`LISTA DE PROFISSIONAIS COMPLETA`, `GERAL`, `CONTATO` da JOSLAINE). Best-effort, nunca
    pré-requisito, falso positivo é o erro barato (escala a mais).
  - **Consumidor final na fase 1** → sempre contato novo → rodízio. O agente não adivinha
    vínculo cliente↔consultora. A consultora reconhece o próprio cliente ao pegar o chamado.
    Memória do agente (Supabase) entra quando o schema existir.
  - **Pergunta do modo** → deliberada, turno 1, duas categorias nomeadas. Sem resposta: se o
    cliente age como consumidor final, segue; se aparece sinal de projeto/profissional,
    escala. Tenta no máximo mais uma vez.
- **`CONTEXT.md`** — entrada `Atendimento qualificado` ajustada: cai "cliente conhecido →
  chamado para a consultora dona" para consumidor final.
- **`map.md`** — a linha do 010 em "Decisions so far" ganhou um parágrafo de addendum.
- Contexto anterior (004, 003, `.gitignore`) permanece — ver `handover-2026-08-30.md`.

---

## Sessões do dia

### Sessão em curso — Addendum do ticket 010 (grilling)

- Fronteira revisada. Recomendação aceita pelo dono: fechar primeiro o addendum do 010 (ponta
  solta, já começada), depois puxar o 017.
- **Grilling do addendum do 010, 2 rodadas (5 perguntas).** Fechado com as três decisões acima
  (lookup → diretório de arquitetos; consumidor final sempre contato novo; pergunta do modo
  deliberada) + o fallback de "sem resposta" + confirmação de que o ticket fica `closed`.
- Executado:
  - `010`: seção `## Addendum` reescrita e completada (estava "em andamento"). Substitui a
    linha "já é cliente" da tabela de campos, a subseção "Contato que já é cliente da casa" e a
    célula da pergunta do modo.
  - `CONTEXT.md`: entrada `Atendimento qualificado` ajustada.
  - `map.md`: parágrafo de addendum na linha do 010.
  - Este handover criado.
- **Próximo:** ticket 017.
