# Handover — 2026-09-12 · Agente Lais Aliski Casa

Projeto: Agente de WhatsApp da Lais Aliski Casa · Branch: `wayfinder/atendimento-hoje` ·
Última atualização: 2026-09-12

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` → este documento
> → `handover/handover-2026-09-11.md` (contexto anterior) → `wayfinder/README.md`.

## Estado atual do repositório

Branch `wayfinder/atendimento-hoje`, sincronizada com `origin` (`c663066` é o commit mais
recente empurrado). **A sessão começou com o checkout principal 34 commits atrasado do
`origin`** (parado em `7d024c3`, de 2026-09-11) — sincronizado via `git reset --hard
origin/wayfinder/atendimento-hoje` antes de qualquer trabalho novo (working tree estava
limpo, o único commit local exclusivo já tinha sido reconciliado à mão numa sessão anterior).
Ver "Sessões do dia" para o detalhe.

> **⚠️ Mudança de plano, meio da sessão**: o WhatsApp deslogou (item 2 antigo), foi
> reconectado — mas **no número PESSOAL do dono, não no da loja**, por decisão explícita dele
> ("quero testar no meu número" primeiro). `ALLOWED_JID` também mudou: não é mais a Larissa,
> é a **Consuelo** (`966485139573@lid`, nome confirmado via `pushName`). **O número real da
> loja segue desconectado desde que deslogou** — ninguém voltou a vincular o `/qr` nele.
> Não confundir "o agente está respondendo" (está, no número pessoal) com "o agente está no
> número real da loja" (não está agora).

1. **O agente está no ar, conectado e validado — no número PESSOAL do dono (teste), não no da
   loja.** `https://agente-runtime-production.up.railway.app/health` responde
   `"status":"conectado"`. **Em MODO TESTE**: variável `ALLOWED_JID` restringe respostas a um
   único contato (**Consuelo**, `966485139573@lid`) — qualquer outro número é ignorado em
   silêncio, e o log agora mostra `pushName`/`fromMe` de quem foi ignorado (achado do dia: sem
   isso, trocar de contato de teste virou garimpo de log bruto do Baileys por 40+ minutos).
2. **Três funcionalidades novas no ar** (pedido do dono, mesma sessão, depois de resolver o
   WhatsApp): botão **"Devolver ao agente"** na plataforma (ao lado de "Devolver à fila"),
   **"Fechar chamado" agora reinicia o atendimento** (próxima mensagem do cliente é
   qualificação do zero, não retomada), e **telefone `@lid` resolvido best-effort** (antes
   mostrava "LID:..." na plataforma). Detalhe técnico e limitações no ticket
   [045](../wayfinder/tickets/045-devolver-chamado-ao-agente.md) e no addendum do
   [012](../wayfinder/tickets/012-quando-e-como-o-agente-escala.md). **Nada disso foi validado
   de ponta a ponta ainda** — só deployado e conferido que o bundle/RPC existem.
3. **`HANDOFF_INSERT_SECRET` rotacionado DE NOVO** (segunda vez no dia — primeira foi a da
   worktree órfã, de manhã). Desta vez o vazamento foi meu: consultei a definição da função
   `handoffs_insert` via Management API pra saber a assinatura antes de adicionar um parâmetro
   novo, e a função guarda o segredo comparando direto no código SQL (não em cofre separado) —
   `pg_get_functiondef` devolveu o valor em texto na minha própria ferramenta. Rotacionado de
   novo, aplicado nas duas funções (`handoffs_insert` e a nova `handoffs_status_for_jids`) e no
   Railway, gerado e aplicado num só passo pra nunca reimprimir. **Risco registrado pra não
   repetir**: qualquer função Postgres que compara segredo direto no corpo (padrão usado desde
   o 031) vaza o segredo pra quem inspecionar a definição dela — vale considerar mover pra uma
   tabela/vault separado quando o 044 mexer nisso de novo.
4. **Bug real encontrado e corrigido, WhatsApp deslogado nunca gerava QR novo** — quando a
   sessão desloga (`DisconnectReason.loggedOut`), o código só logava e parava, nunca limpava a
   credencial velha nem tentava de novo — `/qr` ficava preso em "Gerando QR novo…" pra sempre.
   Corrigido: limpa `AUTH_DIR` e reinicia `start()` nesse caso.
5. **Bug real encontrado e corrigido**: `AUTH_DIR` tinha sido gravado como
   `C:/Program Files/Git/data/auth` (mangling do MSYS/Git Bash em qualquer variável que começa
   com `/`, não só no `volume add` — o mesmo contorno de double-slash precisa ser aplicado a
   *toda* variável de ambiente com valor de caminho absoluto neste ambiente Windows). Efeito:
   a sessão do WhatsApp nunca ia pro Volume persistente, sumia a cada redeploy. Corrigido
   (`AUTH_DIR=//data/auth`) e **validado de verdade**: um redeploy de teste reconectou sozinho,
   sem pedir QR novo.
6. **Página `/qr` corrigida para se atualizar sozinha** — o primeiro pareamento falhou
   ("não é possível conectar o aparelho" no celular) porque a página antiga só trocava de QR
   com reload manual, e o WhatsApp expira o QR a cada 20-60s; nenhuma tentativa de pareamento
   chegou a aparecer nos logs do servidor, confirmando que o QR escaneado já estava morto.
   Agora a página consulta `/qr-data` a cada 4s.
7. **Migração pendente do dia 11 (`agent_settings_public_read.sql`) aplicada em produção** —
   sem o bloqueio de permissão que travou a sessão anterior (bookkeeping da migração
   `20260911180000` precisou de `supabase migration repair` primeiro, porque tinha sido
   aplicada via Management API, não pelo CLI).
8. **`HANDOFF_INSERT_SECRET` rotacionado (primeira vez do dia, de manhã)** — o valor antigo apareceu em texto (achado ao
   investigar por que a variável não estava no `.env` principal: estava presa no `.env` de uma
   worktree órfã, `deploy-plataforma-037`). Por decisão do `CLAUDE.md` (§4, "credencial que
   apareceu em texto está comprometida"), gerado valor novo, gravado direto na função
   `handoffs_insert` (`CREATE OR REPLACE FUNCTION`, via Management API) e nas variáveis do
   serviço Railway. Nunca colado em texto nesta conversa.
9. **Dois worktrees órfãos removidos** (`deploy-plataforma-037` e `reconciliar-021-trunk`,
   junto das branches locais `reconciliar-037` e `worktree-reconciliar-021-trunk`) — ambas já
   totalmente contidas no `HEAD` atual, confirmado por `merge-base --is-ancestor` antes de
   apagar. Os diretórios físicos não saíram do disco (permissão negada, processo antigo
   travando) mas o registro do git foi limpo — não afeta nada, só sobra lixo em
   `.claude/worktrees/` pra alguém apagar manualmente quando puder.

**Links de produção, todos ativos agora:**
- Plataforma das consultoras: `https://plataforma-consultoras-production.up.railway.app`
  (já com o botão "Devolver ao agente")
- Runtime do agente: `https://agente-runtime-production.up.railway.app`
  (`/qr` vincular — hoje pareado no número pessoal do dono, não no da loja; `/health` status;
  `/` texto simples)

Tickets: **045 fechado** (construído direto no provisório, mesma sessão), **012 ganhou dois
addenda** (critério do "devolver ao agente" revisado; "fechar chamado" reinicia o
atendimento), **044 ganhou notas** do que a versão definitiva ainda herda. Ver nota de topo
do `wayfinder/README.md` para o resumo técnico completo.

## Pendências / próximos passos

1. **Decidir quando voltar pro número real da loja** — o teste de agora (áudio, ALLOWED_JID,
   as três features novas) está rodando no número **pessoal do dono**, por escolha dele. O
   número real da loja segue deslogado desde o incidente. Antes de vincular ele de novo,
   confirmar com o dono se é pra fazer isso já ou continuar testando no pessoal mais um tempo.
2. **Validar de ponta a ponta as três funcionalidades novas** (nada disso foi testado com
   chamado real ainda, só deployado): escalar uma conversa de teste, **"Devolver ao agente"**
   na plataforma e confirmar que a Manu volta a responder no WhatsApp (poll de 15s, pode levar
   até isso pra reagir); **"Finalizar chamado"** e confirmar que uma mensagem nova do mesmo
   contato depois disso vira qualificação do zero (não lembra da conversa anterior); conferir
   se o telefone aparece resolvido (não "LID:...") pra um contato `@lid` — sabendo que é
   best-effort, pode não resolver sempre.
3. **Validar texto e áudio juntos** com a Consuelo (ou quem for o contato de teste) — o fix de
   áudio da sessão anterior também não teve validação de ponta a ponta ainda.
4. **Sair do modo teste quando o dono decidir** — trocar/remover `ALLOWED_JID` quando a
   validação terminar. Enquanto isso não acontecer, **o agente não atende ninguém de verdade**,
   mesmo estando "no ar".
5. **Coordenar com o ticket 044** antes de qualquer coisa acima virar "definitivo" — o
   `agente-runtime/` continua **provisório de propósito** (estado de conversa em memória, não
   Supabase — perde tudo num restart; sem idempotência na escalada). O 044 segue
   `assignee: Claude`, reivindicado, sem `## Resolução` — não sei se a sessão paralela que o
   reivindicou ainda existe ou já encerrou. O 044 já ganhou notas do que herda desta sessão
   (áudio, poll de handoffs → deveria virar reação direta com estado persistido, telefone
   `@lid`) — ler antes de decidir se evolui o provisório ou constrói o formal do zero.
6. **Risco de segredo em função Postgres registrado, não resolvido** — `handoffs_insert` e a
   nova `handoffs_status_for_jids` comparam o segredo direto no corpo da função; qualquer
   `pg_get_functiondef`/introspecção nelas reimprime o valor em texto (foi exatamente o que
   vazou o segredo hoje, segunda vez no dia). Vale mover pra uma tabela/vault separado quando o
   044 mexer nisso de novo — não é urgente (RLS já protege a tabela), mas é uma armadilha real
   pra quem for debugar essas funções sem saber disso.
7. **`/qr` e `/qr-data` não têm autenticação nenhuma** — qualquer um com o link pode ver o QR
   pendente (achado ao ler `agente-runtime/index.js`, não investigado além de registrar). Baixo
   risco prático (link não divulgado), mas vale considerar antes de circular amplamente.
8. **Diretórios de worktree não apagados do disco** (item 9 da sessão 1) — `permission denied`
   / `invalid argument` ao tentar `git worktree remove --force`. Não bloqueia nada; limpar
   quando der (fechar processos que possam estar com handle aberto ali, depois apagar a pasta à
   mão).
9. **Fronteira sem mudança**: 021 (Instagram — dono pediu pra não mexer agora), 041 (billing,
   sem pressa), 039 (laço de contexto). 038 segue `in-progress`. Ver `wayfinder/README.md` para
   o texto completo.

## Decisões e notas

### Achado do dia — checkout principal ficava para trás de sessões em worktree

O incidente que o `CLAUDE.md` já registrava (seção "Worktrees e sessões paralelas", escrita em
2026-09-11) se repetiu: o checkout principal (`C:/Agente Lais`) ficou 34 commits atrasado
porque as sessões 6–14 de ontem trabalharam em worktrees separadas
(`deploy-plataforma-037`/`reconciliar-021-trunk`) e empurraram para `origin` sem que o checkout
principal desse `pull`. O checklist da seção 6 do `CLAUDE.md` (rodar `git fetch` + `git
status -sb` + `git branch -a` + `git worktree list` no início de toda sessão) só foi seguido
depois que o dono corrigiu o pedido original desta sessão (que tinha começado, por engano, a
tratar o ticket 021 como o assunto — ver abaixo). **A lição prática**: mesmo com o checklist
escrito, a primeira leitura de estado (mapa + README + handover) não é suficiente sozinha —
precisa mesmo rodar os comandos de git antes de responder "o que fazer agora", não só ler
documentos que podem estar descrevendo um estado que o `origin` já superou.

### Correção do usuário — não era sobre o ticket 021

A sessão começou assumindo, pela leitura do handover de 2026-09-11 e do commit mais recente da
branch local (na época, desatualizada), que "o que fazer agora" era o ticket **021** (Instagram
como porta de entrada), porque era o único claim recente visível. O dono corrigiu: o assunto
real era **o QR code de acesso das consultoras**, mencionado na sessão 14 do dia anterior (não
visível na branch local desatualizada). Isso só apareceu depois de sincronizar com `origin` e
ler o handover completo de 11/09 (849 linhas, a versão real estava numa branch órfã).

## Sessões do dia

### 2026-09-12, sessão 1 — Sincronização, migração pendente e deploy do runtime provisório

- Pedido inicial do dono ("verifique no que temos que trabalhar agora") respondido, a
  princípio, com base só na branch local — apontou o ticket 021 como próximo passo óbvio.
  **Dono corrigiu**: "não era sobre o 21, era sobre fazer o QR code de acesso das
  consultoras" — obrigou a investigar o handover mais a fundo, o que revelou o atraso de 34
  commits (ver "Decisões e notas" acima).
- **Sincronizado** o checkout principal (`git reset --hard origin/wayfinder/atendimento-hoje`,
  seguro — único commit local exclusivo já reconciliado antes). `CLAUDE.md` ganhou a seção
  "Worktrees e sessões paralelas" nesse processo (escrita numa sessão anterior, só chegou ao
  disco agora).
- Confirmado com o dono, via `AskUserQuestion` (2 rodadas): nenhuma sessão paralela ativa
  (worktrees travados eram lixo de sessões já encerradas); seguir a sequência completa que o
  handover de ontem tinha deixado pendente (migração → Railway → vincular WhatsApp → validar →
  coordenar com o 044).
- **Migração aplicada**: `supabase migration repair --status applied 20260911180000` (a
  `CREATE TABLE agent_settings` já tinha rodado via Management API na sessão 13, mas nunca
  entrou no bookkeeping do CLI) + `supabase db push` da `20260911190500_agent_settings_public_read.sql`
  — desta vez sem bloqueio de permissão.
- **Achado durante a preparação do deploy**: `HANDOFF_INSERT_SECRET`, esperado no `.env`
  principal, não estava lá — rastreado até o `.env` da worktree órfã
  `deploy-plataforma-037` (cada worktree tem seu próprio `.env`, gitignored, não
  compartilhado). Ao exibir o valor para localizá-lo, ele **apareceu em texto** — por regra do
  `CLAUDE.md`, tratado como comprometido: gerado valor novo, aplicado na função
  `handoffs_insert` via Management API e nas variáveis do serviço Railway, nunca reimpresso.
- **Serviço Railway `agente-runtime` criado** (mesmo projeto `plataforma-consultoras-lais`),
  com Volume persistente em `/data` (`railway volume add -m //data` — o path precisa do
  double-slash no Git Bash do Windows, senão o MSYS mangla `/data` num caminho Windows).
  Variáveis definidas via `--stdin` (nunca em texto puro no comando):  `GEMINI_API_KEY`,
  `HANDOFF_INSERT_SECRET`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_PROJECT_REF`; `AUTH_DIR` e
  `NODE_ENV` direto (não sensíveis).
- **Primeiro deploy falhou** — `railway up` por padrão sobe a raiz do repositório git inteiro,
  não o `cwd`; o Railpack não reconheceu um projeto único no meio do monorepo. **Achado de
  deploy, vale para qualquer serviço novo aqui**: a flag `--path-as-root` resolve
  (`railway up ./agente-runtime --path-as-root --service agente-runtime -c`). Segundo deploy
  passou limpo.
- Domínio público gerado (`railway domain`): `agente-runtime-production.up.railway.app`.
  `/health` confirmado respondendo, freio de mão lido certo (`agentEnabled: true`), status
  `conectando` (aguardando alguém escanear `/qr` — nada foi escaneado por mim, é ação física
  fora do meu alcance).
- **Limpeza de worktrees órfãos**: `deploy-plataforma-037` e `reconciliar-021-trunk`
  removidos do registro do git (branches `reconciliar-037` e `worktree-reconciliar-021-trunk`
  apagadas, ambas confirmadas como ancestrais do `HEAD` atual antes de apagar). Diretórios
  físicos não saíram do disco (erro de permissão) — sobra de limpeza manual, sem urgência.
- `wayfinder/README.md` e `.env.example` atualizados (nota de topo com o estado novo;
  `.env.example` ganhou a seção 5 documentando `HANDOFF_INSERT_SECRET` pelo nome, sem valor).
- **Primeira tentativa de vincular falhou**: dono escaneou o QR e o celular acusou "não é
  possível conectar o aparelho". Diagnóstico pelos logs do Railway — QR sendo gerado e
  expirando em loop (a cada 20-60s, comportamento normal do WhatsApp) mas **nenhuma tentativa
  de pareamento chegou ao socket**, provando que o QR na tela (imagem estática, só trocava com
  reload manual) já tinha expirado no momento do escaneio. Corrigido: `/qr` reescrita pra
  consultar um novo endpoint `/qr-data` a cada 4s via JS, sem depender de reload. Commit
  `4c2c607`, redeploy.
- **Segunda tentativa: sucesso** — `/health` passou a `"status":"conectado"`. Dono pediu, então,
  para restringir a Manu a responder só ao contato de teste dele (Larissa) antes de abrir pra
  clientes reais — mesmo padrão já usado no `manu-live-bridge.mjs` da sessão 14 de ontem (jid
  `97624673771575@lid`, contato pessoal de teste do dono, já registrado nesse arquivo). Implementada
  a variável `ALLOWED_JID` opcional no `agente-runtime/index.js`: filtra mensagens de qualquer
  outro jid antes de tocar em qualquer lógica, loga em `warn` quando ativa. Setada no Railway,
  redeploy.
- **Achado ao verificar se o pareamento sobreviveria a um redeploy** (não bastava "parece
  conectado agora" — o próprio ponto de partida do dia era exatamente essa promessa quebrada
  ontem): o redeploy **pediu QR de novo**, perdendo o pareamento que tinha acabado de funcionar.
  Investigado: `AUTH_DIR` (setado via `railway variable set AUTH_DIR=/data/auth`) tinha sido
  mangled pelo MSYS do Git Bash pra `C:/Program Files/Git/data/auth` — o mesmo problema que já
  tinha mordido o `railway volume add -m /data` mais cedo (resolvido lá com `//data`), só que
  dessa vez o contorno não foi aplicado por engano. `railway volume list` confirmava
  `currentSizeMB: 0.0`, sinal de que nada persistia de fato. Corrigido (`AUTH_DIR=//data/auth`),
  redeploy, **novo pareamento** (o terceiro do dia) e — desta vez sim — **confirmado por um
  segundo redeploy de teste que reconectou sozinho, sem pedir QR**. Fica registrado como lição
  geral: qualquer variável de ambiente com valor de caminho absoluto neste ambiente (Windows +
  Git Bash) precisa do contorno double-slash, não só argumentos de `--mount-path`.
- Dono pediu a lista de links de produção — respondida direto no chat (plataforma +
  runtime, com os três sub-caminhos do runtime); sem necessidade de registrar em arquivo à
  parte, já estão no topo deste handover.
- **Parado aqui, em modo teste (só Larissa) até o dono validar e decidir abrir pra clientes
  reais.** Commits desta sessão: `498390a` (sincronização + deploy inicial), `4c2c607` (fix do
  `/qr`). O fix do `AUTH_DIR`/`ALLOWED_JID` é só configuração de variável no Railway, não gerou
  commit de código além do `ALLOWED_JID` em si — ver próximo commit a seguir a este handover.

### 08h26–09h23 — Áudio, WhatsApp deslogado, troca de contato de teste, botão "devolver ao agente"

Dois pedidos do dono: (1) diagnosticar e corrigir por que a Manu não responde a áudio; (2)
avaliar um botão novo na plataforma, "devolver chamado ao agente", ao lado de "assumir"/
"devolver à fila". Rodada de `/grilling` (skill) antes de qualquer código — duas investigações
em paralelo (subagentes `Explore`) primeiro, pra não perguntar ao dono nada que desse pra achar
no código.

- **Causa raiz do áudio**: `agente-runtime/index.js` e `manu-live-bridge.mjs` só extraíam
  texto de `conversation`/`extendedTextMessage` — `audioMessage` caía num `if (!texto)
  continue` e era descartado **em silêncio**, sem log. Não é falha de transcrição: nunca
  existiu tratamento de mídia no caminho de produção. O protótipo `prototipo-tom-014/run.mjs`
  já sabia montar `inlineData` de áudio pro Gemini (usado manualmente no navegador) — nunca
  tinha sido portado. Achado também: o comportamento esperado já estava **fechado** no ticket
  014 ("entende o áudio do cliente e devolve o entendimento por escrito") e a viabilidade
  técnica já **validada** no 018 (OGG/Opus aceito inline, sem ffmpeg) — não era decisão nova,
  só implementação que faltou.
- **Grilling (10 perguntas, 3 rodadas)** — decisões do dono: corrigir o áudio antes do botão
  (Q1); manter modo teste (`ALLOWED_JID`) até validar os dois (Q2); ticket formal só pro botão,
  não pro bug de áudio (Q3); fix nos dois arquivos, produção e bridge de teste (Q7); aplicar
  agora, direto no runtime provisório, registrando a lacuna no 044 (Q8); sem eco de
  transcrição pro cliente, só processa e responde normal (Q9). Achado que **mudou o escopo do
  pedido do botão**: ele contraria a decisão "definitivo" do ticket 012 (agente nunca mais fala
  depois que consultora assume) — tratado como revisão explícita, não feature solta (Q4);
  critério da exceção: só antes da consultora ter **respondido de fato** ao cliente (Q10,
  confirmado pelo dono explicitamente como opção B depois da rodada de respostas); estado novo
  no enum `handoff_status` em vez de reaproveitar `pending` (Q6); construção espera o **044**,
  não empilha em cima do runtime provisório que só tem estado em `Map`/memória (Q5).
- **Áudio corrigido**: `downloadMediaMessage` (Baileys) baixa o buffer, monta `inlineData` com
  `mimeType`/`data` (base64) igual ao `prototipo-tom-014/run.mjs`, entra no histórico da
  conversa junto do texto. `askManu()` (`agente-runtime/index.js` e `manu-live-bridge.mjs`)
  atualizado pra montar `parts` com texto **e** anexo, não só texto. Commit `d287e85`.
- **Wayfinder**: ticket [045](../wayfinder/tickets/045-devolver-chamado-ao-agente.md) aberto
  (`blocked-by: ["044"]`, design fechado, sem código de propósito); addendum no
  [012](../wayfinder/tickets/012-quando-e-como-o-agente-escala.md) registrando a terceira
  exceção ao "caminho de volta"; `README.md`/`map.md` atualizados (fronteira/bloqueados, névoa
  da "Superfície para as consultoras"). Commit `2d938cf`. **Ambos os commits empurrados pro
  `origin` logo em seguida** (regra do `CLAUDE.md`, incidente de branch órfã já registrado).
- **Deploy do fix**: `railway up ./agente-runtime --path-as-root --service agente-runtime -c`
  — build e deploy limpos (`Deploy complete`).
- **🔴 Achado no `/health` pós-deploy**: `status: "desconectado"`, e os logs do Railway mostram
  `ERROR: Sessão deslogada pelo WhatsApp — precisa vincular de novo em /qr` (`11:29:45` UTC),
  não só "conectando". **Por que acho que não foi o redeploy que causou isso**: o código
  mudado nesta sessão não toca em nada de autenticação/conexão do Baileys (só parsing de
  mensagem e um import); e o log não mostra tentativa de QR novo antes do erro — se o
  `AUTH_DIR` persistido no volume estivesse vazio/perdido, o comportamento esperado seria gerar
  QR novo (`conectando`), não `loggedOut` direto, que é o WhatsApp **rejeitando credenciais que
  existiam**. Ou seja, tudo indica que a sessão foi deslogada do lado do WhatsApp/celular (ex.:
  dispositivo removido em "Aparelhos vinculados", ou expiração) em algum momento **antes**
  deste redeploy — não consegui confirmar a hora exata porque `railway logs --since 8h` só
  devolveu esse único log (histórico do container anterior não ficou disponível pela CLI).
  `railway volume list` mostra `0MB/5000MB` usados, o que é inconclusivo (pode ser só
  arredondamento de um arquivo de credenciais pequeno). **Fica registrado como pendência
  urgente no topo deste handover** — precisa de alguém escanear `/qr` de novo fisicamente, não
  é algo que eu resolvo daqui.
- **Correção do dono, logo em seguida: Q10 era opção A, não B.** A resposta original
  ("concordo com todas, só a 10 eu quero que seja B") já tinha sido registrada nos tickets
  (012 addendum + 045) como "devolve só antes da consultora responder de fato" — o dono voltou
  e corrigiu para **sem restrição**, mesma confiança já dada ao "Devolver à fila" hoje: a
  consultora decide na hora, mesmo que já tenha respondido. 012 e 045 corrigidos (a versão
  errada fica registrada nos dois como decisão revisada, não apagada — rastro do "por que
  mudou"). Efeito colateral bom: **simplifica** a implementação futura do 045 — não precisa
  mais rastrear se/quando a consultora respondeu (sem coluna `advisor_replied_at`, sem o
  runtime observar esse sinal). Dono está escaneando o `/qr` agora — monitorando `/health` em
  background pra avisar assim que reconectar.
- **`/qr` não carregava nada** — dono escaneou e a página ficou presa em "Gerando QR novo…".
  Causa raiz real: no branch de `loggedOut` do `connection.update`, o código só logava o erro e
  **parava** — nunca limpava a credencial velha nem chamava `start()` de novo, então nenhum QR
  novo era gerado, nunca (o processo ficava vivo, `/health` respondia, mas o socket do
  WhatsApp nunca era recriado). Corrigido: limpa `AUTH_DIR` (`rmSync`) e reinicia `start()`
  nesse caso. Deploy, QR apareceu, reconectou.
- **`/health` confirmou `"status":"conectado"`** — WhatsApp real voltou a responder.
- **Pedido: trocar o contato de teste pra Consuelo.** Sem log do jid filtrado (só existia o
  silêncio total do `ALLOWED_JID`), virou garimpo de log bruto e não-decriptado do Baileys por
  quase uma hora — incluindo dois números errados tentados (`554196598203@s.whatsapp.net`,
  coincidência de horário; e um jid `@lid` que vinha aparecendo como "ruído" de mensagem
  travada desde antes). **Achado que resolveu de vez**: o Baileys expõe `msg.pushName` (nome
  que o próprio contato define no WhatsApp dele) e eu não estava logando isso — só o jid.
  Adicionado `pushName`+`fromMe` ao log do modo teste; a próxima mensagem da Consuelo mostrou
  `pushName: "Consuelo Marchi"` no jid `@lid` que eu tinha descartado como ruído — **era ela
  desde o início**, presa num erro de sessão do Signal que se resolveu sozinho depois de um
  `railway restart` (usado no meio do processo porque o socket ficou "zumbi" — conectado mas
  mudo por 10+ minutos, depois de um `unexpected error in 'init queries': Timed Out`).
  `ALLOWED_JID` corrigido pra `966485139573@lid`, redeploy, confirmado funcionando.
- **Revelação do dono**: o número vinculado agora é o **pessoal dele**, de propósito ("quero
  testar no meu número"). Registrado no topo deste handover — o número real da loja segue
  deslogado, sem ninguém ter vinculado de novo ainda.
- **Três pedidos novos do dono, na sequência**: (1) fazer o botão "devolver ao agente" da
  plataforma funcionar de verdade — revertendo a decisão do 045 de esperar o 044; (2) "fechar
  chamado" deve reiniciar o atendimento (cliente que volta depois disso é atendimento novo);
  (3) telefone aparecendo como "LID:..." na plataforma deveria mostrar o número de verdade.
  Investigado e implementado tudo junto (mesma infraestrutura nova: coluna `contact_jid` +
  poll numa RPC secret-gated) — detalhe completo no `## Resolução` do
  [045](../wayfinder/tickets/045-devolver-chamado-ao-agente.md) e nos addenda do
  [012](../wayfinder/tickets/012-quando-e-como-o-agente-escala.md). Decisão de arquitetura
  registrada: **não** usar Realtime pra isso (como o freio de mão faz) — `handoffs` tem PII de
  cliente, abrir `SELECT` pra chave anônima do runtime exporia isso; poll numa RPC que só
  devolve jid+status resolve sem esse risco.
- **🔴 Achado no meio do trabalho: segredo vazado de novo, desta vez por mim.** Consultei
  `pg_get_functiondef` da `handoffs_insert` via Management API pra saber a assinatura exata
  antes de adicionar o parâmetro `p_contact_jid` — a função guarda o segredo comparando direto
  no corpo SQL (não em cofre separado), então a consulta devolveu o valor em texto na saída da
  minha própria ferramenta. Por regra do `CLAUDE.md` §4, tratado como comprometido na hora:
  rotacionado (gerado e aplicado num único passo, nunca reimpresso — inclusive um primeiro
  candidato que **também** vazou sem querer no meio do processo de corrigir isso, descartado
  antes de aplicar), valor novo gravado nas duas funções (`handoffs_insert` e a nova
  `handoffs_status_for_jids`, criadas no mesmo script) e no Railway. Fica registrado como risco
  de arquitetura pra depois (ver "Pendências").
- **Migração `20260912120000_devolver_ao_agente_e_contact_jid.sql` aplicada em produção**:
  `returned_to_agent` no enum `handoff_status`; colunas `returned_by`, `returned_at`,
  `contact_jid`.
- **Build e deploy dos dois serviços**: `advisor-platform/web` (Vite build local confirmado,
  bundle em produção conferido por grep do texto "Devolver ao agente") e `agente-runtime`
  (reconectou sem pedir QR de novo). `railway up` deu timeout de rede (`reqwest error ...
  operation timed out`) três vezes seguidas num dos deploys — o build **continuou rodando do
  lado do Railway** apesar do erro no cliente CLI; resolvido esperando o `railway status` parar
  de mostrar "Building"/"Deploying", não repetindo o comando às cegas.
- Commits: `10b207f` (feat — as três funcionalidades), `c663066` (docs — 045 fechado, addenda
  012, nota 044). Ambos empurrados pro `origin` na hora.
- **Primeiro teste real do dono achou um bug na hora**: clicou "Devolver ao agente", a
  Consuelo mandou outra mensagem 7s depois, a Manu não respondeu. Causa: mensagem chegando nos
  até 15s entre o clique e o próximo poll caía no `if (status === 'escalado') continue` e era
  descartada pra sempre — só a mensagem *seguinte* teria sido respondida. Corrigido na hora:
  guarda a última mensagem recebida como `pendingMessage` enquanto `escalado`, reprocessa ela
  assim que o poll destrava (extraído `processarTurno()` pra reusar a mesma lógica de
  histórico→Gemini→envia→escala nos dois lugares); poll também caiu de 15s pra 5s. Commits
  `ffb5d12` (fix) + `1725c0b` (docs no 045), deployado, confirmado reconectando sem pedir QR.
- **Segunda correção do dono, mesmo teste**: "Devolver ao agente" fazia o chamado **sumir da
  fila** — errado. Uso real: a consultora ganha tempo sem deixar o cliente sem resposta, mas
  precisa poder **reassumir quando quiser**. Corrigido: `Queue.svelte` lista
  `returned_to_agent` junto de pending/assumed; `HandoffCard.svelte` ganhou um terceiro card
  (badge "Com a Manu", botão "Assumir de novo"). Efeito em cadeia no runtime: como o mesmo jid
  pode ter um chamado devolvido em paralelo a um novo (se reassumir e escalar de novo depois),
  a correlação virou **por id do chamado** (não mais por jid — `handoffs_status_for_ids` no
  lugar de `handoffs_status_for_jids`) e o poll passou a detectar reassunção também,
  re-silenciando a Manu nessa hora (senão as duas responderiam junto). **Terceira rotação do
  segredo no dia** (a função precisou ser recriada com assinatura diferente). Commit `9521d7e`,
  deployado, reconectou sem pedir QR. **Efeito colateral aceito**: o redeploy zera o `Map` em
  memória de novo — o chamado de teste anterior (`d8030f3a...`, já `returned_to_agent`) não é
  mais observado pelo poll até o cliente mandar mensagem nova (aí cria estado fresco).
- **Parado aqui, sem validação de ponta a ponta completa** — as três correções desta sessão
  (mensagem perdida, chamado sumindo da fila, reassunção re-silenciando) ainda não foram
  testadas juntas, de ponta a ponta, pelo dono. "Fechar chamado reinicia" e telefone `@lid`
  seguem sem validação nenhuma. Ver "Pendências" no topo.
