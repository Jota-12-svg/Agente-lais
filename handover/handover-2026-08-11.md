# Handover — 2026-08-11 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última atualização: 05h35

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` (vocabulário do
> domínio) → `handover/handover-2026-08-10.md` (contexto de ontem) → este documento →
> `wayfinder/README.md` para puxar um ticket.
>
> ⚠️ **O PR da branch `wayfinder/atendimento-hoje` continua sem abrir.** `gh` ainda não está
> instalado nesta máquina. Corpo pronto no fim do handover de ontem.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch
`wayfinder/atendimento-hoje`, PR ainda não aberto. `main` limpa.

## Pendências / próximos passos

O gargalo continua **humano**, não técnico — ver handover de ontem para a lista completa
(roteiro do 020 às consultoras, exportação das conversas, acesso à planilha/Mainô). O que
muda hoje:

1. **Ticket 002 (Supabase) ganhou inventário, mas segue aberto.** Falta o dono do projeto
   dizer **"pode apagar"** — ver as três perguntas pendentes no próprio ticket. Sem esse
   aval, nada foi (nem vai ser) removido.
2. Fronteira sem alteração: `003`, `004`, `012`, `016`, `017`, `020`, `021` continuam
   abertos e desbloqueados. `016` (research do parceiro Meta) é o próximo candidato óbvio
   para uma sessão sem o dono do projeto por perto — não depende de ninguém.

## Decisões e notas

- **Supabase: recomendo reaproveitar o projeto atual, não criar um novo.** O resíduo do
  projeto anterior é pequeno e nomeado — um schema (`app`, 7 tabelas, 53 linhas ao todo),
  dois papéis (`agent_runtime`, `platform_worker`), 3 migrações. Nada de Edge Function, cron,
  webhook ou config de Auth alterada que justificasse recomeçar do zero. Detalhe completo no
  ticket [002](../wayfinder/tickets/002-limpar-o-projeto-supabase.md).
- **Confirmado: não há dado de cliente da Lais Casa no Supabase.** As 3 lojas do schema
  `app` (`Ótica Visão Clara`, `Ateliê Manola`, `Clínica Bem Estar`) e os 4 clientes são todos
  do projeto anterior — nenhuma é decoração/mobiliário. 3 dos 4 clientes têm UUID
  determinístico de seed (dado sintético); o quarto, criado 3 dias depois com UUID aleatório,
  pode ser teste manual — mesmo assim não é Lais Casa. Fica marcado no ticket para o dono
  decidir se quer olhar antes de apagar.

---

## Sessões do dia

### 05h15–05h35 — Inventário do Supabase (ticket 002)

- Ticket 002 reivindicado (`assignee: Claude`).
- Inventário rodado via **Management API do Supabase** (`database/query`, só-leitura — nenhum
  `drop`/`delete` executado): schemas, tabelas e contagem de linhas, papéis de banco, RLS e
  policies, funções, extensions, migrações aplicadas, Edge Functions, `pg_cron`, webhooks e
  Vault.
- Achado central: só existe resíduo em **um schema (`app`)** e **dois papéis**
  (`agent_runtime`, `platform_worker`) — o resto do projeto está limpo (sem Edge Function,
  sem cron, sem webhook, Vault vazio, `auth.users` e `storage` zerados).
- Resultado registrado por inteiro no ticket, incluindo os dois achados que a pergunta 1 do
  ticket pedia (nenhum dado é da Lais Casa) e uma recomendação para a pergunta 2
  (reaproveitar o projeto, não criar um novo).
- **Ticket segue aberto** — a segunda metade (o `drop`) espera aval explícito do dono do
  projeto, por ser irreversível. Três perguntas pendentes ficaram registradas no próprio
  ticket.
