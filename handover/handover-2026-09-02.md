# Handover — 2026-09-02 · Agente Lais Aliski Casa

Projeto: Agente de WhatsApp da Lais Aliski Casa · Branch: `wayfinder/como-o-agente-soa-014`
· Última atualização: 2026-09-02, ~15h

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` →
> `handover/handover-2026-09-01.md` → este documento → `wayfinder/README.md`.
>
> ## ⏭️ Ação do dono do projeto pendente: **reagir ao protótipo de tom (ticket 014)**
>
> O protótipo v1 está escrito — sete transcrições + especificação de tom, na seção
> `## Protótipo — v1 para reação` do ticket
> [`014-como-o-agente-soa.md`](../wayfinder/tickets/014-como-o-agente-soa.md), e em versão
> página (estilo WhatsApp) em `wayfinder/tickets/014-prototipo-tom.html`. O ticket **só fecha
> quando o dono reagir** às transcrições e responder as **8 perguntas abertas** no fim do
> ticket (nome do agente, política de preço, áudio de saída, velocidade, emoji, tratamento,
> abertura, fidelidade).
>
> Publicar o HTML como **Artifact** (para o dono comentar linha a linha pelo celular) foi
> **barrado pelo classificador de permissão** nesta sessão — refazer com aprovação do usuário
> quando ele estiver por perto.
>
> ## 🧪 Ambiente de teste ao vivo — pronto para usar
>
> `prototipo-tom-014/` na raiz do repo: servidor Node sem dependências que põe o dono
> conversando com a "Manu" de verdade. Sob teste: o `prototipo-tom-014/system-prompt.md`.
> Rodar: `cd prototipo-tom-014 && node --env-file="C:\Agente Lais\.env" run.mjs` →
> `http://localhost:4014`. **Descartável.** O que sair do teste volta para o ticket 014.
>
> Barra do chat: **seletor de modelo** (default `gemini-3.5-flash-lite`; troca p/ 3.6-flash),
> toggle **fora do horário**, toggle **cache de prefixo** (ligado por padrão), **📎** anexar
> imagem/áudio e **🎤** gravar nota de voz. Cada resposta mostra tempo/modelo/tokens/custo;
> `/custos` = painel; `node calibrar.mjs` calibra contra o billing real. **Editar o
> `system-prompt.md` exige reiniciar o servidor** (lê no boot).
>
> **Custo (02/09, medido):** promo de 2026 do `gemini-3.6-flash` não vale nesta conta (billing
> = preço cheio $1,50/$7,50; `pricing.mjs` calibrado). ~80% do custo é o system prompt
> reenviado a cada chamada. **Cache de prefixo implementado:** `gemini-3.6-flash` sem cache
> R$ 0,017 → com cache **R$ 0,005 (–73%)**. `flash-lite`: cache funciona sem desconto. Levers
> validados por 2 subagentes → `prototipo-tom-014/CUSTOS.md`.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**.

**Três sessões em paralelo hoje (02/09):**
- `worktree-wayfinder+como-o-agente-soa-014` — **esta sessão**, ticket 014 (protótipo de tom).
- `worktree-wayfinder+sinal-de-sucesso-013` — ticket 013 (sinal de sucesso), `assignee:
  claude-session-2026-09-02`.
- `wayfinder/atendimento-hoje` — ticket 018 (contrato do LLM), `in-progress`, `assignee:
  sessão 2026-09-02` (mudança não commitada no checkout principal).
- Ticket 027 segue com `assignee: Claude` (pendente com o dono desde 12/08 — chip + dispositivos).

**Commits desta sessão** (branch `wayfinder/como-o-agente-soa-014`, em `origin`): reivindicação
do 014 → protótipo v1 + `014-prototipo-tom.html` → ambiente de teste `prototipo-tom-014/` →
tracking de custo → correção do preço + comparador de modelos → `calibrar.mjs` → CUSTOS.md
(validação por 2 subagentes) → flash-lite default → **cache de prefixo** → **grelha de contexto
+ bookkeeping (032/011/033/009/CONTEXT.md/map)**. Último: ver `git log`.

Sem PR aberto — a branch carrega todo o histórico de `wayfinder/atendimento-hoje` + estes
commits. `.env` intocado (o protótipo lê a chave em runtime, nunca a grava nem loga).
⚠️ Memória: sessões paralelas re-aplicam à trunk **à mão**, não por merge.

## Pendências / próximos passos

1. **Ticket 014** — `in-progress`. Protótipo + ambiente de teste ao vivo funcionando, contexto
   da loja aplicado. Ainda aberto: as 8 perguntas de tom + as 7 transcrições precisam do aval
   do dono; publicar o Artifact quando aprovado.
2. **Ticket 011** — desbloqueado pelo fechamento do 032. Tem `## Direção` com o escopo
   acordado; falta a resolução formal (não bloqueia nada).
3. **Ticket 033** (novo) — laço de contexto; bloqueado pelo 013.
4. **Fronteira** (tirando 013/014/018/027 em andamento): `011`, `015`, `020`, `021`, `030`,
   `031`. Dependentes de ação do dono: 015, 020/021, 030/031.
5. **Quando as conversas reais aparecerem** (003 fechou sem exportação): reler 014 e 010/013.

## Decisões e notas

### Ticket 014 — protótipo de tom, v1 (2026-09-02)

- **Não é decisão fechada** — é rascunho para o dono reagir. O que está proposto:
  - **Nome do agente: "Manu"** (assistente da Lais Aliski Casa). Evitei "Lia" por soar como Lais.
  - **Mensagens completas** (1–3 linhas, no máx. 2 bolhas), **≤1 emoji/mensagem** e **zero
    emoji** em escala/preço/irritação, **resposta em alguns minutos** com "digitando",
    **"você"**, apresenta-se no 1º turno sem se anunciar robô (confirma se perguntarem),
    **promete a loja nunca a pessoa**, **nunca afirma disponibilidade nem preço**, **pergunta
    do modo no turno 1** com as duas trilhas nomeadas.
- **Sete transcrições** cobrindo: contato frio, arquiteto com planilha, "vocês têm?", preço de
  item caro, escalonamento (+ variante fora do expediente), áudio/foto de outro lugar, cliente
  irritado / desconfiado de robô.
- **Perguntas abertas registradas no ticket** — a mais estrutural é a nº 2: hoje o agente
  **nunca** dá preço na fase 1 (nem faixa), o que trava toda pergunta de preço até o 011/032.

### Contexto anterior

Ver `handover-2026-09-01.md` (tickets 010-addendum e 017) e `handover-2026-08-12.md` (ticket
027, pendente com o dono).

---

## Sessões do dia

### 2026-09-02, ~20h–21h — Rename mecânico + upload de mídia no protótipo

- **Rename mecânico concluído:** "Lais Casa"/"Laís Casa" → "Lais Aliski Casa" em 38 arquivos
  (CLAUDE.md, CONTEXT.md, map, README, todos os tickets e research, prototipo, `.env.example`).
  Standalone "Laís" (a dona) → "Lais". Handovers até 09-01 = registros datados, ficaram.
  (`[íi]` no sed não pega o `í` multibyte — usei `perl -CSD`.)
- **Upload de imagem e áudio no chat do protótipo:**
  - **📎** anexa imagem/áudio (máx ~15 MB, múltiplos); **🎤** grava nota de voz (MediaRecorder).
  - Vai pra Gemini como `inlineData`; conta na `in` da linha cinza (`📎N` marca o turno).
  - `system-prompt.md` ganhou seção "Mídia do cliente": foto de produto → descreve e escala
    (não identifica); foto de ambiente → usa e qualifica; print de lista → arquiteto, escala;
    áudio → devolve o entendimento por escrito.
  - `run.mjs`: `turnosCliente` monta `parts` com texto + `inlineData`; guarda de tamanho 18 MB.
  - **Testado:** imagem (PNG) e áudio (WAV) aceitos pela API, sem erro, comportamento certo.
- Corrigido bug de doc: o README dizia "servidor relê o system prompt a cada mensagem" —
  na verdade lê no boot; editar exige **reiniciar**.

### 2026-09-02, ~19h–20h — Grelha de contexto (o dono testou e a Manu "não tinha contexto")

Rodada de `/grilling` com o dono depois do teste ao vivo. Saiu (tudo **já aplicado**):

- **Nome da loja: `Lais Aliski Casa`** (sem acento em "Lais"). O antigo "Lais Casa" era apelido errado.
  Dona = **Lais Aliski**, **não faz atendimento**.
- **Time = 3 consultoras** (Pamella, Gabi, Joslaine), rodízio puro. **Corrige o 009** (dizia 4).
- **Local:** Curitiba, Batel, R. Francisco Rocha 707. Site + Instagram no `CONTEXT.md`.
- **Mainô não tem catálogo** → **ticket 032 fechado**, **011 desbloqueado** (ganhou `## Direção`,
  segue `open`). Produto do agente = categorias + posicionamento + link do site (não navega).
- **Regra nova:** a Manu nunca diz "não sei" → "vou verificar com a consultora". Fechamento de
  pedido → sempre consultora.
- **Ticket 033 aberto** — "laço de contexto" (contexto do agente evoluindo com atendimentos,
  ≠ sinal de sucesso/fracasso), bloqueado pelo 013.
- **Arquivos tocados:** `prototipo-tom-014/system-prompt.md` (reescrito), `CONTEXT.md` ("A loja"
  + "Consultora" + "Disponibilidade" + "Catálogo"), tickets 009/011/032, novo 033, `map.md`,
  `wayfinder/README.md`, ticket 014.
- **Rename mecânico feito:** "Lais Casa" → "Lais Aliski Casa" em todo o repo (CLAUDE.md, map,
  README, tickets, research, prototipo, `.env.example`). Handovers até 09-01 ficaram como
  estão (registros datados).
- **Testado ao vivo:** a Manu dá endereço certo, deflete "quais são as consultoras?" sem
  recitar, reconhece "quero a Gabi" e escala, manda site+IG quando o cliente quer explorar.

### 2026-09-02, ~18h–19h — Cache de prefixo implementado no protótipo

- `prototipo-tom-014/cache.mjs` + wiring no `run.mjs`: toggle "cache de prefixo" na barra
  (ligado por padrão). Cria um `cachedContents` com o **system prompt estático**; a data/hora
  vai em `contents` (a API rejeita `systemInstruction` junto de `cachedContent` — testado).
  TTL 30 min, recria on-boot/on-404, apaga no Ctrl+C, varre órfãos pelo displayName. `/cache`
  mostra o estado.
- **Medido:** `gemini-3.6-flash` — sem cache R$ 0,0172/resposta, **com cache R$ 0,0046 (–73%)**.
  Multi-turno: `cachedContentTokenCount` constante em 1.675 (o hit aguenta o histórico crescer
  — lacuna #3 do CUSTOS.md **confirmada**). `flash-lite`: cache funciona, sem desconto (rate
  no `pricing.mjs` = entrada).
- Também confirmado por teste: `cachedContents` **cria com 1.675 tokens** (mínimo doc de 4.096
  não aplicado) — o system prompt atual já é cacheável.

### 2026-09-02, ~17h–18h — flash-lite no protótipo + validação dos levers (subagentes)

- **Modelo do protótipo trocado para `gemini-3.5-flash-lite`** (a pedido do dono, para testar
  tom + custo). Desacoplado do `LLM_MODEL` do `.env` (que segue `gemini-3.6-flash` para
  produção): agora o default é hardcoded no `run.mjs`, override via `PROTOTIPO_MODEL` ou o
  seletor no chat. R$ 0,0038/resposta (vs ~R$ 0,018 do 3.6-flash).
- **Dois subagentes voltaram, sintetizados no `CUSTOS.md` reescrito** + `CUSTOS-api-referencia.md`
  (backing detalhado). ⚠️ O subagente de projeto **fabricou resultados atribuídos ao ticket
  018** (que está `open`, não trabalhado neste branch) — raciocínio dele aproveitado, números
  falsos descartados. Os fatos de API foram **re-verificados** por WebFetch + teste real com a
  chave.
- **Achados verificados (teste real 2026-09-02):**
  - `cachedContents` **cria com 1.675 tokens** nos dois modelos (o mínimo doc de 4.096 não foi
    aplicado) → o `system-prompt.md` atual já é cacheável. Hit devolve `cachedContentTokenCount`.
  - `gemini-3.5-flash-lite`: cache **funciona na API mas o preço diz "Not available"** →
    provável sem desconto. `pricing.mjs` corrigido (cacheHit do lite = preço de entrada).
  - default de thinking do `gemini-3.6-flash` = **`medium`** (não "dinâmico") → setar `minimal`
    explícito é **obrigatório** em produção.
  - Batch confirmado ~50%; lite batch = $0,15/$1,25.
- **Veredito dos levers:** fazer cedo (baixo risco) = cache de prefixo + disciplina "prefixo
  estável × contexto dinâmico" + debounce. Não fazer agora = modelo mais barato (mapa já
  rejeitou 2×) e resumo de histórico (risco sobre o gatilho de escala do 012). Distante =
  Batch. Sequência completa no `CUSTOS.md`.
- **Risco de arquitetura registrado:** o `## Contexto agora` que o `run.mjs` cola dentro do
  `systemInstruction` **não pode** virar padrão do runtime — mataria o cache. Data/hora e
  estado da loja têm de ir em `contents`.

### 2026-09-02, ~16h–17h — Correção do custo + comparador de modelos

- Dono comparou com o billing real: subiu ~2× mais que a estimativa. **Causa:** a promo de
  2026 do `gemini-3.6-flash` ($0,75/$3,75) não está sendo aplicada a esta conta — o billing
  roda no **preço cheio ($1,50/$7,50)**. Confirmado testando `thinkingLevel` minimal vs high
  (com `minimal`, `thoughtsTokenCount` é 0 — thinking não é a causa) e relendo a doc de preço.
- `pricing.mjs` agora usa preço cheio por padrão; knobs `COST_CALIBRATION`, `GEMINI_PRICE_IN/OUT`
  no `.env`; o preço usado aparece no banner, na barra e no `/custos`.
- Corrigido bug de matching: `gemini-3.5-flash-lite` casava com `gemini-3.5-flash` (prefixo).
- **Seletor de modelo** no chat (3.6-flash / 3.5-flash-lite / 3.7-flash) — compara tom e custo
  na mesma conversa. `lite` = ~R$ 0,004/resposta vs ~R$ 0,018 do 3.6-flash.
- Número final: ~R$ 22–30/mês a 10 atendimentos/dia — dentro do research 017 §11.4. Não é caro.
- `calibrar.mjs`: mede a estimativa contra o billing real. Medição sintética com os números
  do dono (4 msg, billing 0,88→0,95) → fator ×0,92: **a correção pro preço cheio está certa**,
  a estimativa fica dentro de ~8% do billing.
- **Onde o custo vai:** ~80% dos tokens de entrada é o `system-prompt.md` (~1.676 tokens)
  reenviado a cada chamada — a API não tem memória. A mensagem de texto do cliente é ~30
  tokens. Em produção o system prompt é cacheado (10× mais barato, research §5) → conversa de
  4 turnos cai de ~R$ 0,07 pra ~R$ 0,03. `/custos` mostra esse breakdown.
- **`prototipo-tom-014/CUSTOS.md`** — nota de referência pro desenho do runtime (névoa "Stack
  e hospedagem" do mapa): salvar contexto no Supabase **não** baixa custo de API (modelo é sem
  estado); os levers reais são cache de prefixo (≥4.096 tokens — o prompt cruza isso quando o
  catálogo dos tickets 011/032 entrar), debounce de mensagens, resumo em vez de histórico cru,
  `flash-lite`, Batch pro laço de aprendizado. Estimativa somando tudo: ~R$ 4–15/mês.

### 2026-09-02, ~15h–16h — Tracking de custo no ambiente de teste

- A pedido do dono: `prototipo-tom-014/pricing.mjs` (preços do research 017 §11.1, câmbio
  ~R$ 5,50) estima o custo de cada chamada a partir do `usageMetadata`.
- `run.mjs` grava `custos.jsonl` (uma linha por chamada, com `conversationId`), acumula por
  sessão, e serve `/custos` (painel HTML) e `/custos.json`. `custos.mjs` = mesmo resumo no
  terminal. Barra de custo no topo do chat (sessão + conversa + nº de respostas).
- Cada resposta agora mostra `$… · R$…` na linha cinza.
- Medido em 3 chamadas de teste: ~R$ 0,008/resposta, ~US$ 0,0015. `thoughtsTokenCount`
  voltou 0 com `thinking minimal`. Projeção grosseira do painel ~R$ 9/mês — abaixo dos
  ~R$ 28 do research §11.4 porque não há cache de prefixo nem tool calls aqui (documentado).
- `custos.jsonl` no `.gitignore`. `pricing.mjs` é reaproveitável para o runtime de produção.

### 2026-09-02, ~14h–15h — Ambiente de teste ao vivo

- A pedido do dono: (1) `014-prototipo-tom.html` copiado para `dados/` (fora do git — LGPD);
  (2) montado `prototipo-tom-014/` — servidor Node (sem deps) + UI de chat estilo WhatsApp;
  o dono digita como cliente, a Manu responde via Gemini.
- `system-prompt.md` destila os tickets 009 / 010 (+ addendum) / 012 / 014 em instruções da
  Manu — **é o artefato sob teste**; editável, o servidor relê a cada mensagem.
- Testado contra a API real: chave do `.env` OK (`serviceTier: standard`), campo do
  `thinking_level` que funcionou = `generationConfig.thinkingConfig.thinkingLevel` (as outras
  duas formas ficam de fallback no código). Registrado no ticket 014 e útil para o **018**.
- 4 cenários provados: saudação fria (pergunta do modo no turno 1 ✓), "vocês têm?" (não
  afirmou disponibilidade ✓), arquiteto (escala imediata, só nome + escritório ✓).

### 2026-09-02, ~13h–14h — Protótipo de tom (ticket 014)

- Verificada a fronteira contra o trabalho paralelo: 013 (worktree ativo, *locked*), 018
  (`in-progress` no checkout principal) e 027 (assignee) já tinham dono. **014** era o único
  da fronteira desbloqueado, central e executável sem depender do dono — recomendado e aceito.
- Worktree `wayfinder/como-o-agente-soa-014` criado a partir de `main`; ticket reivindicado
  (`status: in-progress`).
- Lidos map, `CONTEXT.md`, tickets 009 / 010 (+ addendum) / 012 e a 1ª rodada do 020.
- Skill `/prototype` consultada: como o ticket pede transcrições e "nenhum código", o
  artefato durável é o markdown do ticket; a página HTML é o extra reagível.
- Escritas as **7 transcrições + especificação de tom (13 dimensões) + 8 perguntas abertas**
  na seção `## Protótipo — v1 para reação` do ticket 014.
- Criado `wayfinder/tickets/014-prototipo-tom.html` — versão página, estilo conversa de
  WhatsApp, tema claro/escuro, para leitura no celular.
- **Publicação como Artifact barrada pelo classificador de permissão** — registrado como
  pendência; o HTML fica no repo.
- `README.md`: 014 saído da fronteira, com nota. Ticket **não** foi para `closed` nem para o
  `Decisions so far` do mapa — depende da reação do dono.
