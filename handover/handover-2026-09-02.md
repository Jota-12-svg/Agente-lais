# Handover — 2026-09-02 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última
atualização: 2026-09-02, ~02h.

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` → este documento
> → `handover/handover-2026-09-01.md` (contexto do 017) → `wayfinder/README.md`.
>
> ## ⏭️ Sem recomendação forte de ticket — a Fronteira agora depende do dono
>
> Duas sessões correram em paralelo em 2026-09-02 e fecharam **018** e **013**. O que sobra na
> Fronteira (`014` em andamento, `015`, `020`, `021`, `027`, `030`, `031`, `032`, `033`)
> depende de **input do dono** ou das consultoras. Numa das sessões o dono foi consultado e
> **preferiu não abrir `014`/`020` naquele momento**; na outra ele dirigiu o fechamento do `013`.
> Os dois tickets que uma sessão mais avança sozinha:
> - **`015` (rotação de credenciais)** — a parte de decisão dá para fechar já (ordem de
>   rotação; `KIE_API_KEY` deve ser **removido** e não rotacionado — o 017 tirou a kie.ai do
>   projeto); a execução nos painéis vira checklist para o dono. "Onde as credenciais moram em
>   produção" fica bloqueado na névoa de runtime.
> - **`021` (Instagram porta de entrada)** — a decisão já está tomada; falta preparar os links
>   `wa.me` rastreados + mensagens pré-preenchidas + o passo a passo do Meta Business Suite.
>   Entregável fechado, sobra o dono publicar.
> - **`014` (protótipo do tom)** está em andamento noutra sessão; alimenta o `033`.
>
> 📌 **027 — substrato de teste discutido e adiado.** O dono levantou usar um número VoIP
>   (br.did) e depois o WhatsApp pessoal para o teste do 027. Conclusão (NÃO formalizada no
>   ticket, a pedido do dono — "deixar isso de lado no momento"): **nenhum dos dois serve**; o
>   caminho é chip pré-pago dedicado com 2–4 semanas de warm-up antes do Baileys. Detalhe em
>   "Decisões e notas · Ticket 027". Retomar quando o 027 voltar à mesa.
>
> ✅ **PR #1 (`wayfinder/atendimento-hoje` → `main`) está ABERTO** desde 2026-08-10 e acumula
> todo o wayfinder — o aviso "não foi aberto" dos handovers anteriores estava desatualizado.
> `gh` instalado e autenticado (v2.97, `Jota-12-svg`). PR #2 (`wayfinder/017-gemini-billing`)
> também está aberto — provável duplicata/resíduo, confirmar com o parceiro se pode fechar.
> **PR #3** (`wayfinder/sinal-de-sucesso-013` → `wayfinder/atendimento-hoje`) carrega o
> fechamento do 013.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch `wayfinder/atendimento-hoje`,
`main` atrasada (todo o wayfinder acumula nesta branch, via PR #1).

**Commits de 2026-09-02, já em `origin/wayfinder/atendimento-hoje`:**
- `abe15c1` — fecha o **018**: `## Resolução` + `status: closed`; addendum de `.env.example`,
  nota no research 017 §10, linha no `map.md`, `README.md`, `.gitignore` ignora `.claude/`
- `6082264` — handover do 018
- `6ebdee6` — handover: Fronteira revisada com o dono + substrato do 027

**Fechamento do 013 — chega via PR #3** (branch `wayfinder/sinal-de-sucesso-013`, worktree
isolado que rodou em paralelo ao 018): um commit, rebaseado sobre o trabalho do 018 já
publicado (conflitos de `map.md` e deste handover resolvidos na sessão). Quando o PR #3
mergear, o 013 entra na trunk e a branch pode ser apagada.

**Não commitado / fora do git:** `.env` — o 018 corrigiu `LLM_THINKING_LEVEL` de `low` para
**`minimal`** (nunca vai para o git). Untracked: `.claude/` (config local). Scratchpad do 018
tem os scripts de teste (`call.py` + curl) — não versionados.

## Pendências / próximos passos

1. **Fronteira agora:** `014` (em andamento), `015`, `020`, `021`, `027`, `030`, `031`, `032`,
   `033`.
   - **Bloqueados:** `011` (por 032), `019` (em pausa).
   - Ver o quadro no topo: só `015` e `021` avançam bem sem o dono no loop.
2. **Ticket 033 — Superfície das consultoras para o agente** (aberto pelo 013). Onde e como a
   consultora recebe a escalada, julga o atendimento (`advisor_verdict`) e assume a conversa.
   Design, não construção. Não bloqueia nada; depende de ouvir as consultoras (020) e do
   protótipo de tom (014). **Não reabre o 029** (e-mail continua a notificação da fase 1).
3. **Teste de qualidade `gemini-3.6-flash` × `gemini-3.5-flash-lite`** (~20–30 casos reais:
   áudio com sotaque, planilha fotografada, aderência à regra de escalar). **Espera dados de
   conversa reais**, que ainda não existem (003 fechou sem exportação). Não é ticket ainda.
   O 018 confirmou que o contrato dos dois modelos é idêntico — a troca é uma linha.
4. **Faturamento fechado do mês** — única forma de confirmar 100% que áudio não tem sobretaxa
   no `gemini-3.6-flash` (a contagem de tokens já indica que não).
5. **Latência no host de produção** — o 018 mediu um baseline (Brasil, residencial: texto
   ~1,2 s p50, áudio 30 s ~2,6 s p50), mas o host ainda não foi decidido (Névoa do mapa).
6. **027 — quando voltar:** decidir se a conclusão de substrato (chip dedicado + warm-up;
   VoIP e WhatsApp pessoal descartados) vira nota na `## Resolução` ou numa seção de método
   do ticket. O dono pediu para não mexer no ticket agora.
7. **Pendências de dado do dono/consultoras:** exemplo de nota fiscal (pergunta 25 do 020 —
   agora só calibra implementação, **não muda o 013**), exemplos de bons atendimentos
   (pergunta 32), rotação de credenciais (015), Instagram (021), chip + dispositivos (027 —
   ver handover 2026-08-12 e a nota de substrato abaixo), credencial do Mainô (032), acesso de
   edição ao Google Sheets (030/031 — vem no setup na loja).

## Decisões e notas

### Ticket 013 — sinal de sucesso, fechado (2026-09-02)

Fechado por grilling com o dono (3 rodadas). Resolução completa no próprio ticket. Resumo:

- **Duas camadas.** Fase 1 mede a **qualidade da qualificação** (Camada 1: consultora assumiu
  sem recomeçar, dados certos, classificação certa, escalou na hora certa). O **desfecho de
  negócio** (Camada 2: venda/visita/perdido) é registrado como contexto de **peso baixo**,
  nunca alvo de treino na fase 1. Vira alvo na fase 2.
- **Taxonomia.** `terminal_state` ∈ { `escalado`, `resolvido_sem_escalada`, `esfriado`,
  `fora_de_escopo` } + `business_outcome` ∈ { `virou_venda`, `virou_visita`, `sem_venda`,
  `perdido` } quando escala.
- **Captura.** Automática onde dá: escalada (o agente sabe), timer de esfriamento, polling de
  `notas_fiscais_emitidas` no Mainô **best-effort** (assume perder casos de CPF / ciclo longo —
  é reforço, não sinal único). Manual e num canal só: `advisor_verdict` da consultora.
- **`advisor_verdict`** ∈ { nulo, `agente_mandou_bem`, `agente_atrapalhou` } + nota livre. **É o
  único sinal que treina a qualidade do agente** e o de **maior peso**. Pode vir quase sempre
  nulo enquanto o 033 não existir — aceitável.
- **Sinal negativo — decisão deliberada (revertida uma vez no grilling e reafirmada):**
  `sem_venda` / `perdido` / `esfriado` são **neutros**, não falha do agente. Tratá-los como
  sinal negativo ensinaria o agente a **evitar cliente difícil**. Nada de análise automática de
  causa de fracasso na fase 1. Freio registrado: **escalar nunca é falha.**
- **Atribuição:** sem rateio. O atendimento é a unidade; o desfecho é atributo dele.
- **Satisfação:** nunca perguntada ao cliente na fase 1. Inferida do tom
  (`conversation_sentiment`), + `advisor_verdict`, + reclamação explícita → `insatisfação`.
- **Número único de 3 dias** para `esfriado` e para a **janela de retomada** pós-escalada
  (instrução da nota do ticket; arredonda o "2–3 dias" da consultora). Addendum no ticket 012.
- **Campos no Supabase (ponto de partida):** `terminal_state`, `business_outcome`,
  `advisor_verdict`, `advisor_verdict_note`, `conversation_sentiment`, `escalated_at`,
  `last_contact_at`, `closed_at`, `sale_link`. Schema completo segue como névoa no mapa.
- **`CONTEXT.md`** ganhou `Sinal de sucesso` e `Atendimento esfriado`; a subseção "Ainda sem
  definição" ficou sem pendências.
- **Abriu o 033.** Atualizou map.md (linha em Decisions, 2 itens de névoa), README.md, e deixou
  addendum no 012 (janela de retomada = 3 dias) e nota no 020 (pergunta 25 não muda mais o 013).

### Ticket 018 — contrato do LLM validado por chamada real (2026-09-02)

Testado com `curl`/HTTP cru contra `generativelanguage.googleapis.com/v1beta`,
`gemini-3.6-flash`, chave do `.env`. Todas as chamadas: `serviceTier: "standard"` (tier
pago). Detalhe completo e comandos na `## Resolução` do ticket 018.

**As duas consequências arquiteturais — as duas caíram para o lado simples:**

- **Function calling funciona em HTTP cru.** Ciclo de três `POST`. **Regra dura:** o
  `thoughtSignature` que vem em cada part `functionCall` **tem de voltar verbatim** no turno
  `model` do histórico — omitir é **HTTP 400** ("missing a thought_signature"), não
  degradação. Parts de **texto** não exigem isso. O cliente de LLM só precisa guardar e
  reenviar a string opaca; nenhum SDK necessário.
- **Áudio OGG/Opus do WhatsApp entra inline sem transcodificar.** `audio/ogg` e `audio/opus`
  aceitos direto. **O runtime não precisa de ffmpeg/binário nativo por causa de áudio** —
  some a restrição que a Névoa do mapa registrava; serverless volta a ser opção por esse
  lado (o que ainda empurra para processo longo é o self-hosted do WhatsApp, 016/027).

**Contrato — o que o cliente de LLM precisa saber:**

- Endpoint `POST /v1beta/models/gemini-3.6-flash:generateContent`, header
  `x-goog-api-key`, **síncrono** (streaming é `:streamGenerateContent?alt=sse`, não um flag
  no corpo). Para a fila de qualificação: **não-streaming**.
- Raciocínio: campo **`generationConfig.thinkingConfig.thinkingLevel`** (camelCase,
  aninhado). `thinking_level` solto → 400. Valores: `minimal|low|medium|high` (`off` → 400).
  **`minimal` = 0 tokens de pensamento, ~2 s; `low` = ~540 tokens (cobrados como saída) e
  ~5,6 s.** Por isso o `.env` foi corrigido de `low` para `minimal`.
- `systemInstruction` funciona. Saída estruturada (`responseMimeType` +
  `responseSchema`) devolve JSON limpo — é o caminho da extração de campos do 010.
- **HEIC do iPhone passa direto** (`image/heic`). PNG/JPEG/HEIC: ~1064 tokens para imagem
  A4-ish, igual nos três.
- **Context caching explícito funciona em HTTP cru.** `POST /v1beta/cachedContents` →
  `cachedContent` no `generateContent` → `DELETE`. **Mínimo real = 1024 tokens** (não 4096
  como o research 017 assumiu) — o system prompt é cacheável mesmo em ~1,5k tokens.
- **`gemini-3.5-flash-lite` (plano B): contrato idêntico**, latência de texto menor
  (~0,7 s), default de thinking já `minimal`. Trocar = uma linha (`LLM_MODEL`).

**Divergências doc × realidade (registradas no research 017 §10):**

| Doc | Medido 2026-09-02 |
|---|---|
| `thinking_level` no topo de `generationConfig` | `generationConfig.thinkingConfig.thinkingLevel` |
| Áudio 32 tok/s | **25 tok/s** |
| Inline ≤ 20 MB | corpo de ~108 MB passou |
| Mínimo de cache 4096 | **1024** |

**Não fecha (não era escopo):** qualidade `3.6-flash × flash-lite` com dado real; RPM/TPM
do projeto; faturamento fechado; latência no host de produção.

### Ticket 027 — substrato de teste: VoIP e WhatsApp pessoal descartados (2026-09-02)

Conversa com o dono, **não formalizada no ticket a pedido dele** ("deixar isso de lado no
momento"). Fica aqui para a próxima sessão não reabrir do zero.

O dono tem um número VoIP da **br.did** (fixo, (69) 3185-0309, plano "Ilimitado", SIP/App
BR Fone) e perguntou se dava para usá-lo, ou o WhatsApp pessoal, no teste do 027 em vez de
comprar um chip.

- **Número VoIP (br.did) — não serve.** É fixo e VoIP: (1) o WhatsApp faz lookup de operadora
  e recusa muitos DIDs no registro (pode nem registrar; se registrar, só por ligação); (2)
  número VoIP já é, por si, muito mais propenso a banimento — um ban no teste não isolaria a
  variável "self-hosting", que é o que o 027 quer medir; (3) não se parece com o número da
  loja (celular 9 dígitos + companions). Pode ter outro uso no projeto (linha de voz), não
  este.
- **WhatsApp pessoal — não recomendado.** Seria bom substrato em fidelidade (número antigo,
  de operadora, sessão assentada — e o research 028 mostra que sessão nova sofre mais). Mas
  o research 028 catalogou banimento/logout forçado **só ao vincular a sessão, antes de
  qualquer mensagem** — inclusive em número antigo (Evolution #2497) e conta pessoal de 1+
  ano (whatsmeow #561). Risco baixo-moderado, mas o pior caso (perder conversas/grupos do
  número pessoal) é desproporcional a economizar ~R$ 15 de chip.
- **Caminho recomendado:** chip pré-pago dedicado → registrar no WhatsApp Business →
  **deixar esquentar 2–4 semanas** (vinculado ao Web, alguma conversa real) → só então rodar
  o Baileys. Testa numa sessão assentada, que é o cenário real da loja, sem expor nada que
  dói perder. O 027 não é urgente (só bloqueia produção lá na frente).

### Contexto anterior

017 (provedor + modelo), addendum do 010 — ver `handover-2026-09-01.md`. 004, 003 — ver
`handover-2026-08-30.md`. Ticket 027 pendente com o dono — ver `handover-2026-08-12.md`.

---

## Sessões do dia

### 2026-09-02, ~00h30–01h30 — Ticket 018 fechado (contrato do LLM)

- Puxado o **018** (recomendado pelo handover do 017). Reivindicado, `status: in-progress`.
- Bateria de testes `curl`/HTTP contra a Gemini API com a chave do `.env`
  (`gemini-3.6-flash` e `gemini-3.5-flash-lite`). Assets de teste gerados com `ffmpeg`
  (OGG/Opus) e Pillow + `pillow-heif` (PNG/JPEG/HEIC). Scripts no scratchpad.
- **Sete pontos do ticket respondidos** + as duas consequências arquiteturais decididas
  (function calling utilizável; áudio sem ffmpeg). Ver "Decisões e notas".
- Descobertas que corrigem o research: campo `thinkingConfig.thinkingLevel`, áudio 25 tok/s,
  cache mínimo 1024, teto de 20 MB não observado.
- **`.env` corrigido:** `LLM_THINKING_LEVEL` `low` → `minimal` (fora do git).
- `## Resolução` escrita no ticket 018, `status: closed`. Atualizados: `.env.example`
  (comentário do bloco LLM + var `LLM_THINKING_LEVEL=minimal`), research 017 §10 (nota de
  atualização), `map.md` (linha em Decisions so far + bullet de stack/hospedagem sem a
  restrição de ffmpeg), `README.md` (018 para Fechados). Commits `abe15c1` + `6082264`.
- **Próximo:** sem ticket executável sozinho na Fronteira — depende do dono (ver topo).

### 2026-09-02, ~01h30–02h — Fronteira revisada com o dono; substrato do 027

- Apresentada a Fronteira ao dono. Recomendação da sessão era **013** (sinal de sucesso) via
  grilling; alternativas **014** e **020**. **O dono preferiu não abrir nenhum ticket nesta
  conversa** (o 013 acabou fechando na sessão paralela).
- O dono perguntou sobre montar o setup do número do WhatsApp (ticket 027) — explicado que
  027 já está reivindicado, o harness está pronto (`whatsapp-self-hosted-test/`), e o que
  falta é a preparação física (chip + dispositivos).
- Discussão de substrato: número VoIP da br.did e WhatsApp pessoal — **os dois descartados**
  (ver "Decisões e notas · Ticket 027"). **Não formalizado no ticket a pedido do dono.**
- **Sessão encerrada pelo dono com `/clear`.** Nada de código mudou nesta parte; só handover.

### 2026-09-02, ~00h50–02h30 — Ticket 013 fechado (sinal de sucesso) + consolidação do PR

- Sessão começou com o dono pedindo recomendação de próximo ticket, já que outra sessão ia
  pegar o 018. Recomendado o **013** (maior alavancagem, desbloqueado, sem colisão com o 018).
- Worktree `wayfinder/sinal-de-sucesso-013` criado a partir de `6a3450b` para isolar do 018.
- **Grilling do 013 com o dono, 3 rodadas** (Q1–Q6, depois Q7–Q11, depois Q12–Q14). O dono
  reverteu a própria posição no sinal negativo no meio do caminho — decisão final: fracasso de
  negócio é **neutro**.
- **013 fechado.** `## Resolução` escrita, `status: closed`. Criado o **ticket 033**
  (superfície das consultoras). `CONTEXT.md`, `map.md`, `README.md` atualizados; addendum no
  012; nota no 020.
- **Consolidação (a pedido do dono):** descoberto que o **PR #1 já estava aberto** (os
  handovers anteriores diziam que não). A branch do 013 foi **rebaseada sobre o trabalho do
  018** já publicado (conflitos de `map.md` e deste handover resolvidos) e virou o **PR #3**
  (`sinal-de-sucesso-013 → atendimento-hoje`) para revisão do parceiro.
- **Próximo sugerido:** `015` (rotação, parte de decisão) ou `021` (Instagram, preparação) —
  ver quadro no topo. `014` já está em andamento.
