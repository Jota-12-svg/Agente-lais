# Handover — 2026-08-12 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branches: `wayfinder/atendimento-hoje` (sessões da
manhã) + `wayfinder/017-gemini-billing` (esta sessão) · Última atualização: 10h58

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` (vocabulário do
> domínio) → `handover/handover-2026-08-11.md` (mudança de arquitetura de WhatsApp, mais longo)
> → este documento → `wayfinder/README.md` para puxar um ticket.
>
> ✅ **Ticket 012 fechado — desenho do escalonamento.** Ver "Decisões" abaixo antes de mexer em
> qualquer coisa de roteamento, fila ou handoff.
>
> ✅ **Ticket 020 ganhou o texto bruto do Levantamento 1** dentro do próprio ticket, sem
> conteúdo novo — era a mesma resposta já distribuída em 2026-08-11.
>
> ✅ **Ticket 030 fechado — modelo Gemini escolhido por comparação real:
> `gemini-3.5-flash-lite`.** Substitui `gemini-3.6-flash` no ticket 017. Ver "Decisões" abaixo.
>
> ✅ **Ticket 017 fechado — chave criada, billing confirmado, chave testada com chamada real
> (`HTTP 200`, `serviceTier: "standard"`).** `GEMINI_API_KEY` e `LLM_MODEL=gemini-3.5-flash-lite`
> no `.env` local. **Desbloqueou o ticket 018**, que passa a ser candidato de fronteira. Ver
> "Decisões" abaixo para o histórico completo (o modelo foi corrigido duas vezes no mesmo dia).
>
> ⚠️ **PR da branch `wayfinder/atendimento-hoje` continua sem abrir**, e agora também o da
> `wayfinder/017-gemini-billing` — `gh` não autenticado nesta máquina nas duas sessões. Links
> `pull/new/...` de ambas as branches ficaram prontos no output do `git push`.

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. `main` limpa. **Duas branches
ativas hoje:**

- `wayfinder/atendimento-hoje` — sessões da manhã (tickets 012, 020), sincronizada com
  `origin` em `68664dd`, PR ainda não aberto.
- `wayfinder/017-gemini-billing` — esta sessão, criada a partir de `main`, dois commits
  pushados (`2076c20` reivindicação do ticket, `a0fb4c4` correção do modelo Gemini). PR ainda
  não aberto (mesma causa: `gh` sem auth).

Nenhum conflito entre as duas — tocam arquivos diferentes, exceto `wayfinder/map.md`, onde
esta sessão só editou a linha do ticket 008 (não mexida pela outra branch hoje).

## Pendências / próximos passos

1. **Ticket 012 fechado** — ver "Decisões" abaixo para o desenho completo. Abriu o ticket 029
   e criou dependência nova no 013.
2. **Ticket 029 aberto, sem assignee** — research sobre canal de notificação da fila de
   chamados (não pode ser WhatsApp ativo). Candidato principal a testar: regra de notificação
   nativa do Google Sheets. Só é totalmente testável depois do ticket 004 (acesso real à
   planilha).
3. **Ticket 013 ganhou uma dependência nova do 012** — o limiar de "contato perdido" agora
   serve para dois propósitos (sinal de aprendizado e janela de retomada pós-handoff). Ao
   resolver o 013, decidir um único número resolve os dois usos.
4. **Ticket 027 ganhou peso extra** — além dos cinco pontos já registrados, agora também
   precisa responder se um comando digitado pela consultora no próprio chat (mensagem de
   companion) é visível ao Baileys, porque é o mecanismo cogitado para o **freio de mão**
   (adiado do 012 para lá).
5. **Ticket 020 segue vivo, sem mudança de conteúdo — só de forma.** O texto bruto adicionado
   nesta sessão paralela é o mesmo Levantamento 1 já distribuído em 2026-08-11 (mesmo carimbo
   07:46:10). Continuam faltando as outras três respostas (2 consultoras + a dona), a pergunta
   18b (agenda), a 31 (arquiteto comprando pra casa) e os dois anexos (nota fiscal, exemplos de
   atendimento).
6. **Ticket 030 fechado.** Comparação real dos candidatos vivos da família Gemini (research em
   `wayfinder/research/030-modelo-gemini-ideal.md`) — recomendação aplicada no ticket 017.
   Achado que fecha uma dúvida antiga do research 008: `gemini-3.1-pro-preview` **tem**
   function calling na API nativa da Google (a limitação encontrada era só do wrapper da
   kie.ai). Pendência que sobrou, válida para qualquer modelo: formato OGG/**Opus** do
   WhatsApp segue sem confirmação na doc da Google (só documentam "OGG Vorbis") — item do
   ticket 018.
7. **Ticket 017 fechado.** Wizard rodado até o fim pelo dono do projeto: chave criada, billing
   vinculado (confirmado por print do painel — "My Billing Account", "Nível 1 · Pós-pagamento"),
   `GEMINI_API_KEY`/`LLM_MODEL=gemini-3.5-flash-lite` no `.env` real. Validado com chamada real
   à API (`generateContent`), não só pelo painel: `HTTP 200`, `serviceTier: "standard"`. **Um
   contratempo no meio do caminho**, registrado no ticket: a primeira tentativa de colar a
   chave corrompeu o valor (múltiplos colamentos concatenados na entrada oculta do wizard, mais
   um caractere de controle) — diagnosticado pelo tamanho/formato do valor no `.env`, sem
   nunca exibir a chave, e corrigido reexecutando o wizard com um paste só. Fica registrado como
   modo de falha conhecido de `ask_secret`/entrada oculta em terminal.
8. **Ticket 018 desbloqueado, agora candidato de fronteira.** Deve testar contra
   `gemini-3.5-flash-lite`. Prioridade máxima do ticket: ciclo completo de function calling
   (o `thoughtSignature` já apareceu numa resposta com `thinkingLevel: "low"` no teste de
   sanidade desta sessão — ponto de partida) e o formato de áudio OGG/Opus do WhatsApp, que
   segue sem confirmação em nenhuma doc da Google consultada até agora (research 008 e 030).
9. **Fronteira agora:** `003`, `004`, `015`, `018`, `020`, `021`, `027`, `029` — confirmada
   igual em `README.md`, `map.md` e neste handover; nada dessincronizado entre os três.

## Decisões e notas

- **Ticket 030 fechado — modelo Gemini pinado trocado para `gemini-3.5-flash-lite`
  (2026-08-12).** A correção anterior do ticket 017 (`gemini-3.6-flash`) tinha sido escolha
  por eliminação, não comparação — o research 030 comparou de verdade os candidatos vivos
  (`gemini-3.6-flash`, `gemini-3.5-flash`, `gemini-3.5-flash-lite`, `gemini-3.1-flash-lite`,
  `gemini-3.1-pro-preview` como teto). Achados: os dois Flash-Lite desta geração **não cortam
  multimodal** (áudio, imagem incl. HEIC, function calling e thinking todos suportados);
  `gemini-3.1-pro-preview` **tem** function calling na API nativa (a limitação do research 008
  era só do wrapper da kie.ai); `gemini-3.5-flash-lite` preferido a `gemini-3.1-flash-lite`
  (~25% mais barato) porque só o primeiro tem `thinkingLevel` confirmado na doc oficial.
  **Preço oficial confirmado na fonte (não estimado)**, tier Standard pago:
  US$0,30/1M tokens de entrada (mesmo preço p/ texto, imagem, vídeo e áudio) e US$2,50/1M de
  saída — contra US$1,50/US$7,50 do `gemini-3.6-flash`. Custo estimado (500 atendimentos/mês,
  mesma metodologia do research 008): ~R$87/mês, ~75% mais barato. Pendência que sobra pra
  qualquer candidato: formato OGG/**Opus** do WhatsApp sem confirmação na doc da Google — item
  do ticket 018. Detalhe completo no [research
  030](../wayfinder/research/030-modelo-gemini-ideal.md) e na "Atualização — 2026-08-12 (2)"
  do [ticket 017](../wayfinder/tickets/017-provedor-de-llm-e-billing.md).

- **Ticket 017 — modelo Gemini corrigido para `gemini-3.6-flash` (2026-08-12, superado pelo
  030 acima).** O `gemini-3-flash-preview` fixado pelo research 008 foi aposentado pela Google
  em 2026-07-15, cinco dias antes do lançamento do 3.6 Flash — não é mais possível criar chave
  para ele. Achado que continua valendo: ID correto tem **ponto**, não traço
  (`gemini-3.6-flash`/`gemini-3.5-flash-lite`, nunca `gemini-3-6-flash`).
  Detalhe completo na "Atualização — 2026-08-12" do [ticket
  017](../wayfinder/tickets/017-provedor-de-llm-e-billing.md).

- **Ticket 012 fechado — o agente produz fila, não roteia.** Mudança de desenho mais
  importante do dia: o agente nunca decide quem atende um chamado. Ele lança o chamado numa
  aba nova da planilha compartilhada (linha vermelha = pendente), com o nome da dona anexado
  quando existir — mas **sem trava**, qualquer consultora pode pegar. O rodízio continua
  controlado por elas, exatamente como hoje; o agente só reduz o trabalho manual de registrar
  o chamado. Resolução completa no ticket
  [012](../wayfinder/tickets/012-quando-e-como-o-agente-escala.md).
  - **Gatilhos automáticos:** compra concreta, planilha de arquiteto, pedido de pessoa,
    irritação, negociação de preço. **Não automático:** pergunta de disponibilidade — o agente
    responde com a fórmula da consultora ("vou verificar e te retorno") antes de considerar
    escalar.
  - **Transparência:** o agente anuncia a passagem de forma genérica, sem nome. Quem se
    identifica é a própria consultora ao assumir — revisão pequena da leitura do ticket 009
    ("agente só nomeia depois que ela assumiu": na prática, ele nunca chega a nomear).
  - **Timeout:** silêncio total depois do aviso único. Sem reforço, sem reatribuição — evita
    duplicar atendimento.
  - **Caminho de volta:** definitivo dentro do atendimento, com janela curta de retomada
    (mensagem-eco) e reinício total depois de tempo suficiente. Número exato fica para o 013.
  - **Freio de mão:** adiado de propósito para o 027 — é a mesma pergunta técnica que o item 3
    de lá já testa (mensagem de companion gera evento no Baileys?).
- **Confusão esclarecida na sessão paralela (ticket 020):** o dono do projeto trouxe de volta,
  num arquivo separado (`Respostas consultoras/Respostas.txt`, fora do controle de versão), o
  texto que parecia ser um "primeiro bloco" novo de respostas. Conferido ponto a ponto contra o
  que já estava registrado no ticket 020 e nos sete tickets de origem: **é a mesma resposta já
  processada em 2026-08-11** (mesmo carimbo, mesmas frases, mesmas pendências). Nada de novo
  para distribuir — o texto bruto integral foi guardado dentro do próprio ticket 020, como
  subseção "Resposta bruta (texto integral)", porque até então só existia a versão sintetizada.
- **Achado incidental desta sessão:** a pasta `Respostas consultoras/` (que a sessão paralela
  também encontrou, com conteúdo) não estava coberta pelo `.gitignore` — não bate nos padrões
  `/dados/` nem `/conversas/`. Adicionada ao `.gitignore` por precaução, já que é o lugar óbvio
  para a próxima rodada de respostas do ticket 020 cair, e algumas delas trazem dado pessoal
  (nota fiscal, exemplos de atendimento). É cópia de trabalho do dono do projeto — pode apagar
  quando quiser, não é necessária para o repositório.
- **Trabalho em paralelo na mesma branch, de novo** (mesmo padrão do handover de ontem) —
  reconciliado sem perda por rebase, conflito só no próprio handover.

---

## Sessões do dia

### 08h46–09h29 — Ticket 012: grilling e fechamento do escalonamento

- Fronteira revisada com o usuário (`003`, `004`, `012`, `015`, `017`, `020`, `021`, `027`);
  ticket [012](../wayfinder/tickets/012-quando-e-como-o-agente-escala.md) escolhido — dois
  motivos: era resolvível sem depender de ação externa, e já tinha munição parcial da primeira
  resposta do ticket 020.
- Ticket reivindicado (`assignee: Claude`), commitado (`4fe20f5`).
- Skill `/grilling` conduzida em duas rodadas:
  - **Rodada 1 (8 perguntas):** gatilhos de escalada, roteamento quando a primeira mensagem já
    escala, indisponibilidade da consultora designada, mecanismo de notificação, transparência
    da passagem, timeout, caminho de volta, freio de mão.
  - **Resposta do usuário reformulou o roteamento inteiro:** rejeitou o modelo de "agente
    atribui a um rodízio determinístico" e substituiu por **fila de chamados que as
    consultoras controlam**, vivendo numa aba nova da planilha compartilhada. Trouxe também a
    restrição de que a notificação não pode ser WhatsApp ativo (risco de banimento) e o
    refinamento de que o handoff tem uma janela curta de retomada antes do reinício total.
  - **Rodada 2 (3 perguntas):** canal de notificação (virou ticket de research), se o nome da
    dona na fila é trava ou só informação (confirmado: só informação), e se o limiar da janela
    de retomada se decide aqui ou no 013 (confirmado: no 013).
- Síntese completa apresentada ao usuário para confirmação antes de fechar — aprovada sem
  ressalvas.
- Executado o fechamento: `## Resolução` escrita no ticket 012, `status: closed`; nota de
  dependência cruzada adicionada ao ticket 013 (sem reabrir); ticket
  [029](../wayfinder/tickets/029-canal-de-notificacao-da-fila.md) criado (research, canal de
  notificação da fila); `README.md` e `map.md` atualizados; `.gitignore` protegendo
  `/Respostas consultoras/` por precaução. Commitado (`8373106`).
- **Ao tentar dar `git push`, descobri que o dono do projeto tinha rodado uma sessão paralela**
  (ver bloco abaixo) registrando o texto bruto do ticket 020 — a branch remota tinha divergido.
  Reconciliado com `git rebase`, sem força e sem perda de conteúdo de nenhum dos dois lados;
  conflito só no próprio handover, resolvido juntando as duas narrativas na ordem cronológica
  real. Este documento é o resultado dessa reconciliação.

### 09h05–09h20 — Ticket 020: texto bruto do Levantamento 1 registrado no próprio ticket (sessão paralela)

- O dono do projeto colou o texto integral de uma resposta das consultoras, achando que era um
  bloco novo ainda não processado.
- Comparação carimbo a carimbo e frase a frase confirmou que é a **mesma** resposta já
  distribuída em 2026-08-11 para os tickets 019, 011, 012, 013, 014, e como addendum em 009 e
  006 (fechados). Nenhuma informação nova — evitado o retrabalho de redistribuir.
- Atendido o pedido de deixar o texto bruto registrado **no próprio ticket 020**, que até então
  só tinha o resumo da distribuição, não a fonte primária. Adicionado em bloco de código dentro
  de "Levantamento 1".
- Ticket 020 segue **aberto** (ticket vivo) — aguardando as outras três respostas.

### 09h30–09h35 — Fechamento de sessão: verificação de estado antes do `/clear`

- Pedido do dono do projeto: deixar todas as pendências corretas para poder limpar o contexto.
- Ao entrar, encontrei o checkout principal **no meio de um rebase com conflito** (a
  reconciliação das duas sessões paralelas, ver acima) — não iniciado por esta sessão. Esperei
  e reconferi: já tinha sido resolvido e concluído entre uma checagem e outra (commit
  `68664dd`), sem intervenção necessária aqui.
- Verificação de consistência, sem alterar decisão nenhuma: `wayfinder/map.md`,
  `wayfinder/README.md` e o ticket 012 já refletem o fechamento corretamente; `.gitignore` já
  cobre `/Respostas consultoras/`; `git fetch` confirma `origin/wayfinder/atendimento-hoje` em
  `68664dd`, igual ao local — nada para empurrar.
- Único ponto desatualizado era o **topo deste handover**, que ainda dizia "push não
  confirmado" — reescrito para refletir o estado verificado. Sem mudança de conteúdo além
  disso.

### 09h40–10h58 — Ticket 017: reivindicado, chave criada, modelo corrigido, depois trocado por comparação real (ticket 030), fechado

- Sessão em background, branch nova `wayfinder/017-gemini-billing` (worktree isolado).
- Ticket [017](../wayfinder/tickets/017-provedor-de-llm-e-billing.md) reivindicado
  (`assignee: Claude`), commitado e pushado (`2076c20`).
- Gerado wizard interativo (`setup-gemini-api-key.sh`, fora do repo — é ephemeral) cobrindo os
  três passos que só o dono da conta Google pode fazer: criar a chave, vincular billing,
  confirmar tier pago. `gh` não autenticado, então não abri o PR — link `pull/new/...` ficou no
  output do `git push`.
- **Dono do projeto voltou dizendo que a chave criada é `Gemini 3.6 Flash`, não `Gemini 3
  Flash`.** Investigação (web search) confirmou que não é só nome: `gemini-3-flash-preview`
  (escolha do research 008/ticket 017) **foi aposentado pela Google em 2026-07-15**. Detalhe
  completo em "Decisões" acima e na "Atualização — 2026-08-12" do próprio ticket 017.
- Corrigidos `.env.example`, `wayfinder/map.md` (linha do ticket 008) e adicionada nota (sem
  reescrever o corpo) no topo de `wayfinder/research/008-gemini-kie-ai.md`. Commitado e pushado
  (`a0fb4c4`).
- **Ao ler o `.env` real (não versionado) para checar se `GEMINI_API_KEY` já estava preenchida,
  encontrei o arquivo na estrutura antiga** (seção "KIE.AI", sem campo `GEMINI_API_KEY`,
  `LLM_MODEL` editado no lugar errado com formato errado). **Não editei** — arquivo carrega
  segredos reais de produção (tokens Supabase, senhas de banco), então voltei a pergunta para o
  dono do projeto em vez de reescrever sem confirmação. Ver item 6 de "Pendências".
- Segue conversando com o dono do projeto para reconciliar o `.env` e explicar como validar a
  chave (ticket 018) antes de fechar o 017.
- **Dono do projeto pediu para abrir sub-agente pesquisando o modelo Gemini ideal** (custo +
  áudio + imagem + reasoning sem alucinação). Aberto e reivindicado o ticket
  [030](../wayfinder/tickets/030-escolher-modelo-gemini-ideal.md) (research), disparado
  sub-agente em background (`/research`) com escopo e critérios do ticket. Commitado e pushado
  (`0639d8d`).
- **Sub-agente voltou em ~8 min com research completo**
  (`wayfinder/research/030-modelo-gemini-ideal.md`), recomendando trocar `gemini-3.6-flash`
  por `gemini-3.5-flash-lite`. Ticket 030 fechado, `map.md` atualizado, commitado e pushado
  pelo próprio sub-agente (`9274a8e`). Não mexeu em `.env.example` nem no 017 por instrução
  explícita — deixou a decisão de aplicar para o dono do projeto.
- Dono do projeto perguntou se o custo estimado (500 atendimentos/mês) podia variar bastante
  pra mais ou pra menos — confirmado que sim (depende de duração real dos áudios, nº de fotos,
  nº de turnos por chamado, e se cache de contexto está ativo), mas a comparação *relativa*
  entre modelos se mantém mesmo com essa incerteza, porque todos escalam pelo mesmo padrão de
  uso assumido.
- **Dono do projeto aprovou a troca**, mas pediu preço oficial exato antes de aplicar — "não
  vamos fazer estimativas". Confirmado via `WebFetch` direto em
  `ai.google.dev/gemini-api/docs/pricing`, tier Standard/pago: input US$0,30/1M (texto, imagem,
  vídeo e áudio ao mesmo preço), output US$2,50/1M, cache US$0,03/1M + US$1,00/1M/hora de
  armazenamento — bate exatamente com o que o research 030 já tinha citado.
- Aplicada a troca: `.env.example`, ticket 017 ("Atualização — 2026-08-12 (2)"), `map.md`
  (linha do ticket 008) e o wizard `setup-gemini-api-key.sh` atualizados para
  `gemini-3.5-flash-lite`. Commitado e pushado (`5272331`).
- Orientado o dono do projeto passo a passo em como abrir o Git Bash e rodar o wizard
  (o comando `cd C:\Agente Lais` sem aspas quebrou na primeira tentativa por causa do espaço no
  nome da pasta — corrigido).
- Confirmado pela print do painel do AI Studio que o billing estava vinculado ("My Billing
  Account", "Nível 1 · Pós-pagamento").
- **Primeira tentativa de teste da chave falhou** (`HTTP 400`, resposta HTML genérica do
  Google, não JSON da API). Investigando o `.env` sem exibir o valor, achei o campo
  `GEMINI_API_KEY` corrompido: múltiplos colamentos concatenados na entrada oculta do wizard
  (`ask_secret`/`read -rs`), mais um caractere de controle (`^V`) no início — clássico de
  colar mais de uma vez achando que a entrada oculta (sem eco na tela) não tinha funcionado.
  Orientado o dono do projeto a rodar o wizard de novo, colando uma única vez.
- Segunda tentativa: valor limpo (54 bytes, sem caractere de controle). Teste real contra
  `generateContent` retornou `HTTP 200`, `modelVersion: "gemini-3.5-flash-lite"`,
  `serviceTier: "standard"` — confirma tier pago pela própria API, não só pelo painel. Achado
  incidental pro ticket 018: veio `thoughtSignature` preenchido mesmo com `thinkingLevel: "low"`.
- **Ticket 017 fechado**: checklist marcado, `## Resolução` escrita registrando o achado do
  paste corrompido como modo de falha conhecido, `status: closed`. `README.md` atualizado
  (017 movido para Fechados, 018 movido de Bloqueados para Fronteira, 030 adicionado a
  Fechados) e `map.md` atualizado (entrada de decisão para o 017, entrada do 030 marcada como
  aplicada). Commitado e pushado (`7f03270`).
