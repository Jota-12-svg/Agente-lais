# Handover — 2026-09-10 · Agente Lais Casa

Projeto: Agente de WhatsApp da Lais Casa · Branch: `wayfinder/atendimento-hoje` · Última
atualização: 2026-09-10, ~23h00

> **Para a próxima sessão, nesta ordem:** `wayfinder/map.md` → `CONTEXT.md` → este documento
> → `handover/handover-2026-09-02.md` (contexto anterior) → `wayfinder/README.md`.
>
> ## ⏭️ Próxima sessão: 038 com o grilling feito, mas AINDA ABERTO por decisão do dono
>
> **Estado:** `wayfinder/atendimento-hoje` empurrada (HEAD `228ae40` no momento em que esta
> sessão anexou seu trabalho ao handover errado; ver nota de correção abaixo). **Passo 1:**
> `git pull`.
>
> **O grilling do 038** (estratégia de rollout) foi **feito por inteiro com o dono** (3
> rodadas, 11 perguntas). Decisões gravadas no ticket sob `## Decisões do grilling
> (2026-09-02)`. **O ticket segue `in-progress`, NÃO `closed`** — decisão explícita do dono:
> partes da estratégia ainda dependem de trabalho inexistente (watchdog/SMS ← stack de
> runtime; incremento "reportar problema" ← 037; gate ← 027/011/014). Ver "Pendências para
> fechar o 038" no próprio ticket.
>
> **NÃO propagado** (só quando o 038 fechar): linha no `map.md` Decisions, incremento formal
> no 037, nota no 036, saída de "estratégia de rollout" do bloqueio do 034. O **034 continua
> bloqueado** pelos quatro tickets **+** pela estratégia de rollout (038 aberto). **Não gerar
> o manual.**
>
> **Branches vivas além desta:** `wayfinder/como-o-agente-soa-014` (014, sessão ativa — **não
> tocar**, worktree locked) e `feat/plataforma-consultoras` (037, sessão ativa — **não
> tocar**, worktree locked, HEAD local `884a6f6`, **não empurrada** para origin). Ambas
> conflitarão leve em `README.md` ao integrar.
>
> ℹ️ **PR #1** (`wayfinder/atendimento-hoje` → `main`, aberta) —
> https://github.com/Jota-12-svg/Agente-lais/pull/1. Todo o wayfinder acumula nela; `main`
> segue atrasada por decisão (squash no fim). `gh` instalado e autenticado (v2.97,
> `Jota-12-svg`).

## Estado atual do repositório

Repositório: **https://github.com/Jota-12-svg/Agente-lais**. Branch
`wayfinder/atendimento-hoje`, `main` atrasada (todo o wayfinder acumula nesta branch, via PR #1).

**Commits desta sessão (2026-09-10) em `origin/wayfinder/atendimento-hoje`:**
- `0acf79e` — abre e reivindica o **038** (grilling da estratégia de rollout)
- `4abf5cf` — grava as **decisões do grilling do 038** (ticket segue `in-progress`)
- `228ae40` — handover (anexado por engano ao `handover-2026-09-02.md`; corrigido nesta
  sessão — ver bloco abaixo)
- `b104edb` — corrige o arquivo de handover: restaura o do 09-02, migra a sessão para o do
  09-10, ajusta as datas do grilling no ticket/README/map

**Branches:**
- `wayfinder/atendimento-hoje` — trunk-de-registro, PR #1. **Empurrada.**
- `wayfinder/como-o-agente-soa-014` — **ativa, outra sessão** (ticket 014). Não tocar.
  Worktree locked em `.claude/worktrees/wayfinder+como-o-agente-soa-014`. HEAD local
  `29d8ed7`. Traz `prototipo-tom-014/`, `CUSTOS.md`, cache de prefixo.
- `feat/plataforma-consultoras` — **ativa, outra sessão** (ticket 037). Não tocar. Worktree
  locked em `.claude/worktrees/feat-plataforma-consultoras-037`. HEAD local `884a6f6`
  (reivindica o 037); **não empurrada** para origin.

**Working tree limpo, branch empurrada.**

**Fora do git (inalterado):** `.env` — `LLM_THINKING_LEVEL` em `minimal`. Untracked: `.claude/`.

## Pendências / próximos passos

1. **038 (estratégia de rollout) — grilling feito, ticket ABERTO.** Fecha quando as pontas
   dependentes de outros tickets estiverem amarradas (ver "Pendências para fechar o 038" no
   próprio ticket): watchdog/SMS ← stack de runtime; incremento "reportar problema" + rota
   SMS ← 037; confirmação do gate ← 027/011/014; ajuste da seção do freio de mão do 033/034
   (veículo do aviso = plataforma + e-mail/SMS, não "o grupo"). **Ao fechar:** escrever
   `## Resolução`, `status: closed`, linha no `map.md` Decisions, incremento formal no 037,
   nota no 036, tirar "estratégia de rollout" do bloqueio em prosa do 034.
2. **Manual do agente → ticket `034`.** O `033` fechou a forma; o `038` fechou a forma da
   estratégia de rollout. **034 continua bloqueado** por `011`, `014`, `036`, `037` **+** a
   estratégia de rollout (038 aberto). **Não gerar o manual.** Nada a adiantar nele agora.
3. **Fronteira agora:** `014` (sessão ativa), `015`, `020`, `021`, `027`, `032`. `038` está
   `in-progress` (reivindicado). **Nada mais puxável sem insumo do dono.**
   - **Bloqueados:** `011` (por 032), `019` (em pausa), `034` (por 011/014/036/037 +
     rollout), `031` / `036` / `037` (stack de runtime — névoa do mapa; `037` puxável para
     adiantar schema/protótipo — **e há sessão nele agora**).
4. **Integração das branches paralelas** quando as outras sessões fecharem: `014`
   (`wayfinder/como-o-agente-soa-014`) e `037` (`feat/plataforma-consultoras`). Ambas
   conflitam em `README.md` (Fronteira). A `014` traz `prototipo-tom-014/`, `CUSTOS.md`,
   cache de prefixo, e mexe no ticket 014.
5. **Teste de qualidade `gemini-3.6-flash` × `gemini-3.5-flash-lite`** (~20–30 casos reais).
   Espera dados de conversa reais (003 fechou sem exportação). Não é ticket ainda; a troca é
   uma linha (`LLM_MODEL`), contrato idêntico (018).
6. **Faturamento fechado do mês** — confirma que áudio não tem sobretaxa no `gemini-3.6-flash`.
7. **Latência no host de produção** — 018 mediu baseline; host ainda não decidido (Névoa).
8. **Demais tickets dependem do dono:** 015 (rotação), 020 (perguntas), 021 (Instagram),
   027 (chip + dispositivos — ver handover 2026-08-12 e a nota de substrato no
   `handover-2026-09-02.md`), 032 (credencial do Mainô).

## Decisões e notas

### Correção do arquivo de handover — 2026-09-10

Esta sessão, ao começar, não tinha a data real em contexto e tratou o
`handover-2026-09-02.md` como "o handover mais recente", **anexando nele** o trabalho do
grilling do 038 (commits `0acf79e`, `4abf5cf`, `228ae40`). Ao surgir a data (`2026-09-10`),
o `handover-2026-09-02.md` foi **restaurado ao estado `66b71c8`** (fim da sessão do 033) e
todo o conteúdo desta sessão migrou para este arquivo. As `## Decisões do grilling` **no
ticket 038** ficaram com o rótulo `(2026-09-02)` nos commits já empurrados — é ruído
cosmético; se incomodar, corrigir para `(2026-09-10)` numa próxima passada.

### Ticket 038 — estratégia de rollout, GRILLING FEITO, ticket segue aberto (2026-09-10)

O 034 (redigir o manual) está bloqueado por `011`, `014`, `036`, `037` — todos `open`, e
`014`/`037` em sessões paralelas ativas. O único bloqueio do 034 que é pura decisão, sem
dependência externa e sem colisão, é a **estratégia de rollout** (era névoa do mapa, sem
ticket). Via `AskUserQuestion`, o dono escolheu abrir o grilling dela.

Criado o **038** (`wayfinder:grilling`), reivindicado. Grilling com o dono, **3 rodadas, 11
perguntas**. Decisões gravadas no ticket sob `## Decisões do grilling`. Resumo da forma da
estratégia:

- **Piloto:** agente **24/7 desde o dia 1**, **as três consultoras** desde o dia 1, rodízio
  normal. **Sem madrinha**, sem faseamento, **sem horário restrito** — o dono rejeitou as
  três ideias. Isso **reverte** o "piloto com uma consultora" da névoa do mapa.
- **Comportamento pleno da fase 1 desde o dia 1** (nada estreitado).
- **Duração: 4 semanas** + checagem obrigatória no fim → (a) vira operação normal / (b) +2–4
  semanas de ajuste / (c) rollback. Essa checagem **é** o gatilho de manutenção do manual
  (item 9 do 033).
- **Gate de entrada:** checklist de 9 itens (037, 036 testado, 027, 011, 014, 031, runtime
  hospedado, lógica de qualificação, Parte A + demo). O 038 fixa a lista, não a data.
- **Fallback:** caiu → watchdog avisa **o dono** por SMS/Telegram (nunca consultora), sem
  auto-resposta; errou no ar → canal de erro, dono ajusta; alucinando em série → freio de
  mão (036), consultora puxa na hora.
- **Canal de aviso de erro:** item **"reportar problema"** na **plataforma das consultoras**
  (standalone, não amarrado a chamado) → e-mail (+ SMS no piloto). Aviso do freio de mão cai
  no mesmo lugar. **Sem grupo de WhatsApp** (dono rejeitou). → amplia o escopo do 037.
- **Critério de saída:** julgamento do dono ouvindo as três — veredito majoritário "pronta
  pra assumir", nenhum episódio de credibilidade aberto, as três preferem com agente do que
  sem. **Não** é número de conversão (013: desfecho de negócio é neutro na fase 1).
- **Expansão:** não se aplica (as três + 24/7 desde o dia 1; fase 2 é outro mapa).
- **Rollback:** freio de mão desliga o agente → contatos voltam para as consultoras como
  antes (WhatsApp Business nunca saiu do ar, 016); conversas em curso viram chamado
  imediato; decisão de abortar é do dono.
- **Entrega do manual:** Parte A na semana -1 + demo ao vivo (número de teste, as três + a
  dona); Parte B no dia 0 com a plataforma na tela; consolidar numa sessão só se a
  plataforma permitir.

**Pendências que impedem fechar o 038** (registradas no ticket): watchdog/SMS ← stack de
runtime; incremento "reportar problema" + rota SMS ← 037; confirmação do gate ←
027/011/014; ajuste da seção do freio de mão do 033/034 (veículo do aviso = plataforma +
e-mail/SMS, não "o grupo").

**Por decisão do dono, o 038 NÃO foi fechado** e nada foi propagado ao map/034/036/037.

### Contexto anterior

Sessões de 2026-09-02 (018, 013, 035, 033) — ver `handover-2026-09-02.md`. 017, addendum do
010 — ver `handover-2026-09-01.md`.

---

## Sessões do dia

### 2026-09-10, ~21h30–23h00 — Ticket 038 aberto e grelhado (estratégia de rollout)

- O dono pediu para **continuar no 034**. 034 continua bloqueado por `011`/`014`/`036`/`037`
  + estratégia de rollout — todos `open`; `014` e `037` em sessões paralelas ativas
  (worktrees locked). Descoberta a branch `feat/plataforma-consultoras` (037), que o
  handover do 09-02 não mencionava.
- Único bloqueio do 034 que é pura decisão, sem dependência externa e sem colisão: a
  **estratégia de rollout** (névoa do mapa, sem ticket). Via `AskUserQuestion`, o dono
  escolheu abrir o grilling.
- Criado e reivindicado o **`038`** (`wayfinder:grilling`), commit `0acf79e`.
- **Grilling com o dono — 3 rodadas, 11 perguntas.** Decisões gravadas no 038 sob
  `## Decisões do grilling`. Commit `4abf5cf`. Ver "Decisões e notas" acima.
- **O dono pediu para NÃO fechar o 038** — a forma está decidida, mas partes dependem de
  trabalho inexistente (runtime, 037, gate). Ticket fica `in-progress`, nada propagado.
- **Erro corrigido no fim da sessão:** o trabalho tinha sido anexado ao
  `handover-2026-09-02.md` (a sessão não tinha a data real). `handover-2026-09-02.md`
  restaurado ao estado `66b71c8`; conteúdo migrado para `handover-2026-09-10.md` (este
  arquivo). Ver "Decisões e notas · Correção do arquivo de handover".
- **Nada de código** — só wayfinder e handover.
- **Arquivos:** novo `038`, `README.md` (Fronteira), `map.md` (bullet de rollout aponta pro
  038), `handover-2026-09-02.md` (restaurado), novo `handover-2026-09-10.md`.
